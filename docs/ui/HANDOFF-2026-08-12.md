# Handoff — 2026-08-12 (session end at `87d0be4`)

Naye session mein yeh poori file paste kar dein.

---

PaperMaker — UI architecture sprint, session handoff

REPO: C:\PaperMaker\paper-maker-mvp   (sirf yeh path. E: drive ki purani copies mat chhuo)
BRANCH: feat/ui-architecture
HEAD: 87d0be4 — remote `backup` par push ho chuka, tree clean, dev server band, port 8000 free

KAAM KARNE KA TAREEQA (yeh sab se ahem hai):
- Har qadam se pehle ruk kar mujh se ek-lafzi "go" lo. "Aage kya karna hai?" poochna ijazat
  nahi hai — qadam batao, phir ruko.
- Files kabhi PowerShell Set-Content/Out-File se mat likho, UTF-8 kharab hota hai. Edit tool
  use karo.
- Push main khud GitHub Desktop se karta hoon. Tum push mat karo, sirf `git ls-remote backup
  feat/ui-architecture` se confirm kar dena jab main kahoon.
- Koi cloud deploy nahi (Railway/Northflank band hain). `backup` remote sirf backup hai.

ABHI TAK KYA HUA (aakhri teen commits):
  6691704  D33 paper par verify, do findings
  35056dc  UI-046 ka pehla qadam = measurement, aur scope
  87d0be4  wo measurement chal gayi, aur usne do cheezein hila deen   <- HEAD

87d0be4 ke nataij (tafseel docs/ui/NEXT-SESSION.md mein UI-046 wale block mein hai):
1. `.nav a .icon` par layer(components) rule width/height NAHI leta — nateeja wahi tha jo
   socha gaya tha, magar dalil ghalat thi. Muqabil app.css:57 ka bare `.icon` nahi hai;
   blueprint par theme.css ka apna `.nav a .ic, .nav a .icon` (18px, 0,2,0, khud bhi
   unlayered) hai. app.css:57 se dobara reasoning karoge to gumraah hoge.
2. BLUEPRINT PAR KOI LAYER HAI HI NAHI. probe ne `layerStatements: []` diya; page sirf
   app.css + theme.css + 99-legacy/blueprint.css load karta hai, main.css kabhi nahi. Yani
   aaj UI-046 ke 12 ke 12 rules inert honge. "11 live + 1 known-dead" UI-047b se pehle naapa
   hi nahi ja sakta.
3. Gate jaisa likha tha wo fail ho hi nahi sakta: `.app`/`.top`/`.nav` chhe live pages par
   0 elements match karte hain (bank, landing, library, print, slo-health, slo — blueprint
   par 3, control ke tor par). To css_type_probe hamesha 0 deltas dega chahe rules durust
   hon ya ghalat. Use no-regression check ke tor par chalao, tasdeeq mat samjho.

KHULA SAWAL — PEHLA FAISLA JO CHAHIYE:
UI-046 vs UI-047b ki tarteeb. UI-046 likha to ja sakta hai magar verify nahi ho sakta jab
tak UI-047b blueprint ko migrate na kar de. Agar sprint ka usool "jo task ek page mukammal
kare" hai, to shayad UI-047b pehle aana chahiye. Maine yeh faisla nahi kiya — doc mein sirf
haqeeqat likhi hai, tarteeb nahi badli. Mujh se poochho.

MIGRATION KI HAALAT: 6 of 9 pages live.
  live (css/pages/*.css):          bank, landing, library, print, slo-health, slo
  legacy (theme.css + 99-legacy):  blueprint, index, taqseem

UI-047b apne saath kya laata hai (UI-046 ke 12 se bahar): 19-name compat block, 3 .brand
rules, 3 partials, aur do media-query rules jinhe kuch redeclare nahi karta —
@media (max-width: 760px) ke .app aur .nav, yani narrow-viewport shell bhi toota hua hai.
Blueprint ko UI-043 (.chip) bhi chahiye.

TOOLS: scripts/css_page_rule_probe.mjs <url> [selector-substring] — har rule kis layer mein
aaya + kitne elements match karta hai. Baqi probes docs/ui/PROBES.md mein.
Server: start-local.bat (uvicorn, port 8000). Probe headless Edge uthata hai, har run mehnga
hai — jahan static HTML se jawab mil sakta ho wahan grep behtar hai.

EK TRAP JO IS SESSION MEIN LAGA: `\b` wali grep `app-sidebar` aur `app-nav` par galat match
deti hai (hyphen word-boundary hai). Class token check karna ho to poora attribute match
karo: class="([^"]* )?(app|top|nav)( [^"]*)?"

PEHLA KAAM: docs/ui/NEXT-SESSION.md padho (UI-046 wala block), phir tarteeb wala sawal
mujh se poochho. Kuch likhne se pehle "go" lo.
