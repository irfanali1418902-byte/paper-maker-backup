# UI-ARCH — Deferred / Parking Lot

Anything found mid-task that is **out of scope for that task** lands here instead of being
fixed inline. This file is the pressure-release valve that makes the "one task, one page,
no drive-by fixes" rule survivable.

**Rule: if you find it and it is not your task, write it here and move on.**
Do not fix it. Do not "just quickly" fix it. Adding a row here is the correct outcome.

Format: `| ID | What | Found in | Why deferred | Proposed home |`

---

## Open

| ID | What | Found in | Why deferred | Proposed home |
|---|---|---|---|---|
| D2 | 31 stale git branches (`feature/*`, `hissa-*`, `fix/*`) | audit 2026-07-28 | git hygiene, unrelated to UI | a `chore/branch-cleanup` session |
| D3 | `docs/SDLC-WORKFLOW.md` §4/§6 still describe Railway auto-deploy | audit 2026-07-28 | Railway/Northflank is dead; Irfan pushes via GitHub Desktop, no auto-deploy | doc fix, after this epic |
| D4 | `IMPLEMENTATION_HANDOFF.md`, `REDESIGN_SPEC_blueprint.md`, `ROLLOUT_all_pages.md` all specify the **Classic navy/gold** palette, superseded by Modern | audit 2026-07-28 | three stale specs will mislead a future session | archive to `docs/design/archive/` in UI-064 |
| D5 | `mockup-modern.html` + `papermaker-mockup.html` are served as live app URLs | audit 2026-07-28 | they are design references, not pages | move to `docs/design/` in UI-064 |
| D7 | `.btn.gold` renders **teal** under the Modern palette — the class name lies | audit 2026-07-28 | rename is scheduled, not a bug | UI-041 (button component) |
| D8 | `landing.html` exists but `/` still serves `index.html`; landing is unreachable in normal use | audit 2026-07-28 | routing/product decision, not styling | ask Irfan after the epic |
| D9 | `print.html` and `landing.html` never loaded `theme.css` at all | audit 2026-07-28 | resolved naturally by the migration | UI-013 / UI-018 |
| D10 | Repo root holds 11 `paper_maker*.db` backups + `app.zip` / `.claude.zip` / `config.zip` | audit 2026-07-28 | working-tree hygiene; backups should not live in the repo root | a `chore/repo-hygiene` session (ROADMAP H2 already tracks this) |
| D11 | `compare_js_classes` only catches **JS-side** renames. All 32 load-bearing class names appear in a `<script>` body, so the `class="..."` half of the search is doing no work; a class renamed **in markup only**, with stale JS left behind, passes the check. Measured, not theoretical. | UI-002 review 2026-07-28 | closing it needs per-class, per-half baseline state — a design change to `BASELINE.json`, not a tweak. The docstring states the limit honestly, and the frozen id/onclick/data-* inventory (which *is* exact) already covers the attribute half of the threat | a follow-up ratchet task, or fold into UI-050 when inline styles are burned down |
| D12 | CSS built in JS is invisible to every metric — `el.style.cssText = ...`, `sheet.insertRule(...)`, or a template literal with no literal `<style>` tag. No `<style>` element, no `style=` attribute, no `.css` file, so `shared_css_lines` does not close it either | UI-002 review 2026-07-28 | no occurrences today; detecting it reliably means parsing JS, which is well beyond a line-counting ratchet | revisit only if a page starts generating CSS at runtime |
| D13 | `static/brand/logo.svg` hardcodes `fill="#2E5AAC"` — the same Classic blue deleted from the brand config in UI-003 | UI-003 review 2026-07-28 | an SVG cannot read a CSS token or the API, so this is an asset change (recolour or `fill="currentColor"`), not a CSS one. No ratchet metric counts it: the ratchet reads `.html` and `.css` only | UI-064, alongside the other asset/palette reconciliation |
| D14 | `.pg-btn` reads `color: var(--text)`, but `--text` is defined **nowhere** — not in the block, not in `app.css`, not in `theme.css` (grep is empty). The declaration is inert; pagination buttons inherit their colour instead, which happens to look right. Now at `99-legacy/library.css:232` | UI-014 2026-07-29 | pre-existing, not caused by the extraction. Fixing it mid-Sprint-1 would be a real visual change inside a task whose entire contract is *zero* visual change — and the fix is a token decision (`--ink`? `--muted`? delete the line?), not a typo | UI-050/051 inline burn-down, or whenever `.pg-btn` becomes a real component |
| D15 | `blueprint.html`'s `@media (max-width: 760px)` block styles `.app-sidebar`, `.app-nav`, `.sidebar-foot` and `.bp-main` — **none of which exist** in `blueprint.html`, `app.css` or `theme.css` (grep is empty for all four). Dead responsive CSS; the page's actual mobile behaviour comes from the chrome in `theme.css`. Now at `99-legacy/blueprint.css:269-276` | UI-015 2026-07-29 | pre-existing at HEAD, not caused by the extraction. Deleting it mid-Sprint-1 would be a real change inside a task whose contract is *zero* visual change, and it needs a browser check at <760px to confirm nothing actually depends on it | UI-051/052 inline burn-down, or the `99-legacy/` kill in Sprint 6 |
| D16 | `docs/ui/PLAN.md` §Sprint-1 describes `print.html` as having **5 `@media print`** blocks. It has **2**, plus 1 `@page` and 1 `@media (max-width: 760px)`. Verified twice with CSS comments stripped (implementer + review agent) — a raw grep over-counts because the block's prose comments mention `@page` twice and `@media print` once. STATUS.md's UI-017 handoff repeated the same raw-grep error ("3 / 3") and was corrected in UI-018 | UI-018 2026-07-29 | not silently reconciled (§12 rule 11). PLAN.md's Sprint 1 table is now historical — the sprint is done — so the wrong figure misleads nobody mid-flight, and editing another planning doc inside an extraction task is exactly the drive-by §12 rule 2 forbids | UI-064, alongside the other stale-doc fixes (D3/D4/D5) |
| D17 | `docs/ui/PLAN.md` §2 writes its own `01-settings/tokens.css` example as a **single line** (`:root { --indigo-600:#4f46e5; --teal-500:#0ea5a4; ... }`). `test_tokens_file_holds_only_token_hex` (added UI-020a) is line-based — it exempts any line carrying a `--name:` declaration in full — so a session that copies PLAN.md's formatting satisfies the guard **vacuously**, and could park a component rule on the same line undetected. UI-020 therefore authors the real file one declaration per line, which is what makes the check bite | UI-020a review 2026-07-30 | the guard's limitation is now stated honestly in both docstrings rather than oversold, and the file UI-020 actually ships is formatted so the check works. Editing PLAN.md's example is a planning-doc change inside a task that owns neither — the drive-by §12 rule 2 forbids | UI-064, with the other stale-doc fixes; or fold into UI-021 if `02-generic`/`03-elements` need the same formatting rule stated |
| D18 | `test_token_hex_only_exempts_the_one_sanctioned_file` (UI-020a) cannot catch a broadening of `token_hex_count()` that is **derived from `TOKENS_PATH`** — e.g. `TOKENS_PATH.parent.glob("*.css")`. The test monkeypatches `TOKENS_PATH` to a decoy in a tmp dir, so any parent-relative implementation follows the patch into a directory that holds only the decoy and the arithmetic still checks out. Measured by the UI-020a review agent across four mutations: returning `stylesheet_hex_count()`, hardcoding the real path, and a blanket `static/css/**` amnesty are all **caught**; the `.parent.glob` form is **not** | UI-020a review 2026-07-30 | structural to the monkeypatch approach, and it pre-dates the fix — neither the broken nor the corrected assertion ever detected it, so nothing regressed. Practical blast radius is small: the only sibling `01-settings/theme.css` is Tier 2, which must carry no raw hex anyway, so a `.parent` broadening would exempt a file the ratchet already requires to be hex-free. Closing it needs a fixture that plants a **second** decoy alongside the first | a follow-up ratchet task, or fold into UI-021 when `01-settings/` gains its second real file |
| D19 | `main.css` imports **all nine** `99-legacy/<page>.css` files into `layer(legacy)`, so once a page's only stylesheet is `main.css` it will load all nine. They were nine separate per-page `<style>` blocks and they define the same generic class names with different values — `.card` (6 files), the button classes across 5 files (`.btn-ghost` in 5, `.btn-primary` in 3), `.modal` (4), `.page-head` (4), plus `:root` in all 9 — so every page would get every page's version and the **last import would win for all of them**. Visible breakage, not a purity issue | UI-020 2026-07-30 | **not a deviation — `docs/ui/PLAN.md` §2 specifies exactly this** ("`99-legacy/*` is imported **first** so everything in the new tree wins without a single `!important`"), and it has to be an `@import ... layer(legacy)` rather than a `<link>`, because a plain `<link>` is *unlayered* and unlayered styles outrank every `@layer` — linking would invert the cascade `main.css` exists to establish. Harmless today: nothing links `main.css` until Sprint 3, verified by serving the app (0 pages reference it). Fixing it inside UI-020 would mean redesigning the documented load mechanism in a task scoped to authoring tokens, which §12.2 forbids | **UI-031 / UI-032**, which are the tasks that put the single `<link>` on a page and therefore cannot avoid it. Two candidate shapes: scope each legacy file under a `body[data-page="x"]` wrapper, or drop the shared legacy imports from `main.css` and keep the per-page legacy `<link>` inside a `<style>@import url(...) layer(legacy)</style>`-style shim so it stays layered. Decide there, with a browser open |
| D20 | **The `99-legacy/` layer depends on `static/theme.css` for 21 custom properties that `main.css` does not supply.** Measured: the nine legacy files reference **49** distinct custom properties and declare **30** themselves, so **24** must come from outside; `main.css`'s `layer(settings)` satisfies exactly **3** (`--font-display`, `--font-body`, `--font-data`). The other 21 — `--accent`, `--accent-soft`, `--app-bg`, `--brand`, `--brand-2`, `--chip-bg`, `--fg`, `--green`, `--green-bg`, `--panel`, `--panel-2`, `--radius`, `--radius-sm`, `--red`, `--red-bg`, `--sidebar-active`, `--sidebar-bg`, `--sidebar-fg`, `--yellow`, `--yellow-bg` (all from `static/theme.css`) and `--text` (**from nowhere — already inert, that is D14**) — would resolve to nothing. Consumers are `blueprint.css`, `taqseem.css`, `print.css` and `library.css`, but **the live blast radius is 2 pages, not 4** — measured at the UI-020 review: `blueprint.css` loses 17 tokens and `taqseem.css` 20, **none of them fallback-protected**, whereas `print.css`'s only orphan (`--accent`) carries a `#0e4d3c` fallback on all 4 uses *and* `print.html` never linked `theme.css` anyway (D9), so Sprint 3 changes nothing for it; `library.css`'s `--text` is already inert today (D14). So a Sprint 3 session that makes `main.css` a page's only stylesheet and drops the `theme.css` link ships pages whose colours, radii and sidebar all fall back to initial/inherited: **big visible breakage, and no ratchet metric or test would catch it** — the ratchet counts lines and hex, not whether a `var()` resolves | UI-020 review 2026-07-30 | **this is the half D19 does not describe.** D19 is about class-name collisions between the nine legacy files; this is about token *dependencies* pointing at a file the new tree does not replace. A session that reads only D19 will scope the class collisions, drop `theme.css`, and hit this. Fixing it inside UI-020 would mean authoring Tier 2 aliases for 20 legacy token names — component work in a task scoped to the palette, which §12.2 forbids — and the aliases belong wherever the pages are actually migrated | **UI-031 / UI-032**, with D19; they are the same decision. Either keep the `static/theme.css` link until each page's legacy file is drained (Sprint 6), or add a temporary compatibility block mapping the 20 legacy names onto Tier 2 roles and delete it in UI-064. `--text` needs no alias — fix or delete it per D14 |
| D21 | **A "reset" in this tree is not neutral — it overrides the legacy layer.** `main.css` declares `@layer legacy, settings, generic, elements, ...`, and layer order beats selector specificity for normal declarations, so a *one-element* selector in `02-generic`/`03-elements` outranks every *class* rule in `layer(legacy)`. The design target opens with `* { box-sizing:border-box; margin:0; padding:0 }` (`mockup-modern.html:15`) and is right to, having no legacy layer beneath it; ported verbatim the `padding:0` half beats `99-legacy/index.css:267`'s `[dir="rtl"] .question-card ul { padding-right:20px }` and flattens list indentation in Urdu mode. **No ratchet metric or test sees this** — the ratchet counts lines and hex, not whether a list still reads | UI-021 2026-07-30 | UI-021 worked around it rather than deferring the whole problem: the universal padding kill is **not** ported, the margin reset is targeted at block text elements only, and `img` gets `max-width` without the usual `display:block`. Each omission is stated in the file that omits it. What is deferred is the *general* question — every future `02-generic`/`03-elements` rule has to be checked against the legacy layer, and there is no automated guard for it. This is a third failure mode alongside D19 (class-name collisions between the nine legacy files) and D20 (legacy tokens pointing at `static/theme.css`); a session that reads only those two will not expect it. **One consequence is parked here rather than in a row of its own:** `html, body { height: 100% }` is in the design target (`mockup:16`) and in **7 of the 9** legacy files, but it is *not* in `reset.css` — height is layout, which is `04-objects/shell.css`, not one of the three concerns PLAN.md §2 gives that file. The flex/grid app shell needs it, so **UI-030 must land it** or the shell will not fill the viewport — and when it does, the rule newly reaches the **two** files that lack it: `landing.css:18` (has the `html, body` rule, margin only) and `print.css` (no `html, body` rule at all; `print.css:24` uses `display:flex; min-height:100vh` instead). **print.html is the one to check in a browser** — it is the Ctrl+P page under `@page { margin: 0 }` that `STATUS.md` calls the epic's highest visual risk. Counted per file: this row and `reset.css` both said "8 of 9, landing is the exception" in their first draft, which hid the print.html half | **UI-031 / UI-032**, the same browser-open session that resolves D19/D20 — and it is the same decision for the same reason. Worth a targeted check at <760px and in Urdu mode. ~~The `height: 100%` half belongs to **UI-030**~~ — **CLOSED at UI-030 as unnecessary, not deferred again.** The prediction was wrong: the design target carries *both* `html,body{height:100%}` (`mockup:16`) and `.app{height:100vh}` (`mockup:58`), and the second is what fills the viewport — the first is redundant to it. `04-objects/shell.css`'s `.o-shell` uses the same `100vh`, so the shell fills the viewport without the rule, and the print.html risk this row was written to flag is never taken. A later task that genuinely needs percentage heights inside the shell can add it then, with `print.html` open |
| D22 | **The design target's button reset is deliberately NOT in `03-elements/forms.css`.** `mockup-modern.html:19` has `button { font-family:inherit; cursor:pointer; border:none; background:none; color:inherit }`. The last three declarations, landed in `layer(elements)`, would flatten every filled button in the app the moment a page's only stylesheet is `main.css`: buttons are drawn by class in `layer(legacy)` — `.btn-ghost` in five of the nine files, `.btn-primary` in three — and the element rule outranks all of them (D21). `static/theme.css`'s `.langsw button` (:77) and `.segbtns button` (:163) are **not** part of that argument and were wrongly cited as such in this row's first draft: that file is a plain unlayered `<link>`, so it beats every `@layer` while it is loaded and is gone entirely once it is not. Which way a rule resolves depends on whether it arrived layered, which is D19/D20's subject. UI-021 ships only `font-family: inherit; cursor: pointer`. **The `font` shorthand is a fourth trap in the same rule, and UI-021's first review caught it there**: `font: inherit` reads as a synonym for `font-family: inherit` and is not — it also sets font-size, font-weight, font-style, font-variant, font-stretch and line-height, so from `layer(elements)` it drags every legacy button to the body's 15px/400 and strips the weights they set by class (`99-legacy/slo.css:52` `.btn` 14px/600, `:60` `.btn-ghost` 13.5px/600, `bank.css:348` 13px/700, `library.css:118` 13px). It shipped as the shorthand, was caught in review, and is now `font-family` | UI-021 2026-07-30 | landing the appearance reset needs a `.button` component in `layer(components)` to win it back **in the same commit**, which is UI-041's declared scope. Doing it in UI-021 would mean authoring a component inside a task scoped to generic + elements (§12.2), and would leave the tree in a state where Sprint 3 breaks buttons | **UI-041** (`button` component). Add the three declarations there, in the same commit that authors `.button` — and use the longhand for anything you do not mean to reset |
| D23 | **`--font-mono` names a family nothing provides.** `01-settings/tokens.css`'s `--font-mono: "IBM Plex Mono", ui-monospace, monospace` — and `static/theme.css`'s `--font-data`, which predates this epic — name **"IBM Plex Mono"**, but no `@font-face` anywhere declares it and no `IBMPlexMono-*.woff2` is in `static/fonts/` (verified: 9 woff2 files, 4 IBM Plex Sans + 3 Source Serif 4 + 2 Noto Nastaliq Urdu). So `--font-data` always falls through to `ui-monospace` — which looks fine, which is why it has gone unnoticed. Consumers today: `static/theme.css` `.tag` / `.bloom .pct`, and now `03-elements/typography.css`'s `code, kbd, samp, pre` | UI-021 2026-07-30 | the fix is a **font asset decision**, not CSS: either add the two woff2 files (~80KB, and they must be licence-checked and committed) or drop the family from the stack and let `ui-monospace` be the declared intent. Both are Irfan's call, and neither belongs in a task scoped to authoring generic + elements. Not fixed by accident either — `02-generic/fonts.css` absorbs `static/app.css` **verbatim in coverage**, and app.css never had a Plex Mono face | ask Irfan, then either a small asset task or **UI-064** with the other palette/asset reconciliation (D13). Stated in both token files so no session assumes the family loads |
| D24 | **`nav.mypapers` is referenced by `index.html`'s nav markup but has no entry in its translation table.** The table carries `nav.generate`, `nav.syllabus`, `nav.adaptive`, `nav.results`, `nav.analytics` and `nav.settings` in both English and Urdu — `nav.mypapers` is in neither, so the "My Papers" item does not translate when the page switches to Urdu; it keeps whatever the markup hardcodes. Two related oddities in the same table, recorded but not touched: `nav.syllabus` and `nav.results` have translations but are **not** nav items — `results` is a screen reached another way (`showScreen('results')`, 4 call sites), and `syllabus` has neither a nav entry nor a `showScreen` call, though its `data-screen="syllabus"` section is still in the markup, so it is an orphaned screen rather than a deleted one, and the six page links — Question Bank, Image Library, Blueprint, Learning Outcomes, Exam Taqseem, SLO Health — carry no `data-i18n` at all, so they are untranslated on every page today | UI-030 2026-07-31 | found while measuring the real nav to author `config/nav.json`; the config records the key so nothing is lost. Fixing it means editing `index.html`'s markup or its i18n table, and UI-030 authors **no markup and no JS** — a page edit inside a task scoped to a stylesheet plus a config file is exactly what §12.2 forbids. It is also not a CSS defect at all, so no ratchet metric moves either way | **UI-032**, which migrates `index.html` and is already in that file's markup; or a standalone i18n fix at any time — nothing blocks on it. Decide with Irfan whether the six page links should become translatable too, since that is a product question, not a cleanup |
| D25 | **Four nav destinations have no address, and that is why the nav drifted into three versions.** `mypapers`, `adaptive`, `analytics` and `settings` are SPA screens inside `index.html`, shown by `showScreen(...)`; they have no URL, so a page that is not `index.html` has no way to link to them. Measured at UI-030: `bank`/`blueprint`/`library`/`print` carry a 5-item nav, `index` 10, `slo`/`slo-health`/`taqseem` 7, and `landing` none. **Only half of that is explained by the missing addresses, and the first draft of this row wrongly claimed all of it was** (caught in review): the three 7-item pages carry exactly the 7 `url` items, so for them the four unlinkable screens are the whole difference — but the four 5-item pages additionally drop `/slo.html` and `/taqseem.html`, which have perfectly good URLs and are linked from the 7-item pages. So the 5-vs-7 split **is** arbitrary drift, and the 7-vs-10 split is *mostly* structural — not entirely: it is +4 unlinkable screens **and −1 linkable item**, because `index.html` carries no Generator / `/` entry at all while every other page does. That is not a self-link convention either — the 7-item pages do self-link (`slo.html`'s nav includes `/slo.html`, and so on for `taqseem` and `slo-health`). **UI-032 has to decide whether rendering this config on `index.html` adds a Generator item that is not there today.** Both of the tidier claims this row made first — "exactly the items that cannot be linked", then "only the 7-vs-10 split is structural" — were caught in review; the counts were never wrong, the conclusions drawn from them twice were. `config/nav.json` therefore marks each item `url` or `screen` and **declines to invent a convention** for the screen half (`/#mypapers`, `/?screen=mypapers`, or making them real pages are all plausible and all have JS attached) | UI-030 2026-07-31 | UI-030's scope is `04-objects/shell.css` + the nav config as **data**; the option Irfan chose explicitly excludes a JS nav renderer, and picking an address scheme is a routing decision that changes `index.html`'s script, not a layout one. Deciding it here would also pre-empt the task that has to implement it | **UI-032** (it owns `index.html` and its `showScreen` handlers), informed by **UI-031** having already rendered the config on the five simple pages. Until it is decided, a migrated non-index page can only render the 7 `url` items — which is what those pages show today, so nothing regresses |
| D26 | **`.brand small` lands at 3.04:1 on the dark sidebar — under WCAG AA, though it is an IMPROVEMENT on what was there.** `03-elements/typography.css`'s `small { font-size: var(--text-micro); color: var(--color-text-muted) }` is an ELEMENT rule in `layer(elements)`, so it outranks `99-legacy/slo.css:26`'s `.brand small { font-size: 10.5px; color: #9DB0D0 }` — a *class* rule in `layer(legacy)`. That part is D21 arriving for real. Measured in headless Edge 150 on `slo.html`, computed styles plus the **painted ancestor background**, both sides: before `rgb(157,176,208)` at `opacity .7` over **`#ffffff`** — because `static/theme.css`:62 `.brand { background: var(--panel) }` painted a white box inside the navy sidebar — composites to `rgb(186,200,222)` on white = **1.70:1**; after `rgb(100,116,139)` at full opacity on `#16294A` = **3.04:1**. So contrast nearly doubled; it is still under AA's 4.5:1 for text that size, which is why this row exists. The subtitle also loses `text-transform: uppercase`, `letter-spacing: .6px` and `opacity: .7`, all from `theme.css`:67. `--color-text-muted` is slate-500 and is correct for muted text **on the light canvas it was designed against**; the sidebar is the one dark surface in the app and the token has no dark-surface counterpart, so every page UI-031/UI-032 migrates hits it | UI-031 2026-07-31, in the browser | UI-031's scope is one page's `<link>` block plus the entry-file mechanism. The three ways to fix it are all somebody else's task: a `--color-text-on-dark` role is `01-settings` (Sprint 2, closed), narrowing `small` so it sets size but not colour edits a reviewed UI-021 file and changes every page, and the right fix — the sidebar being a **component that owns its own text colours** — is `05-components/nav.css` in Sprint 4. Patching `99-legacy/slo.css` is not an option: that file is append-never | **UI-040/041** (nav + component colours), or a one-line `--color-text-on-dark` token if Irfan wants AA before the sidebar component lands. **This row's first draft FAILED review and deserved to**: it claimed "6.59:1 → 3.04:1, a legibility regression", having measured both colours in the browser and then computed the *before* ratio against an assumed `#16294A` backdrop instead of the white box `theme.css` actually painted. The colours were measured; the backdrop was assumed; the conclusion came out inverted. Same defect class as UI-020's "linked by 8 pages" and UI-030's "12 items" — **check what is painted, not what you expect to be painted** |
| D27 | **`<a class="btn-ghost">` loses its blue; `<button class="btn-ghost">` keeps it — same class, two appearances on one screen.** `typography.css`'s `a { color: inherit }` (element, `layer(elements)`) outranks `99-legacy/slo.css:60`'s `.btn-ghost { color: var(--primary) }` (class, `layer(legacy)`), but nothing touches `<button>`. Measured before vs after on `slo.html`: the "Template download" `<a>` goes `rgb(46,90,172)` → `rgb(15,23,42)`, while `#exportBtn` and `#filterBtn` — same class, same card — stay `rgb(46,90,172)`. So the page now shows three `.btn-ghost` controls in two different colours. `a { color: inherit }` itself is not the defect and must not be reverted: **8 of the 9 legacy files declare exactly that rule**, it is what stops link-buttons rendering as browser-blue underlined links, and `typography.css` says so in its own comment. The defect is that buttons are drawn by class in a layer that a bare element selector outranks — which is **D22's argument, arriving one rule earlier than D22 predicted**: D22 parked `border`/`background`/`color: inherit` from the *button* reset for exactly this reason, and `a`'s colour reaches the same controls through the anchor half nobody parked | UI-031 2026-07-31, in the browser | the fix is the `.button` component in `layer(components)`, which outranks both and paints `<a>` and `<button>` identically — that is **UI-041's declared scope**, and D22 already parks the matching half of it there. Doing it here means authoring a component inside a task scoped to one page's `<link>` block (§12.2), and doing it in `99-legacy/` is barred outright | **UI-041** (`button` component), with D22 — they are one fix. Until then the mismatch is on `slo.html` and on every page UI-031/UI-032 migrates that has an `<a class="btn-ghost">`. **Flagged to Irfan at handover, not shipped silently** |
| D28 | **A migrated page loses its intra-card spacing: `reset.css`'s margin reset zeroes three legacy class margins, while `typography.css` makes the headings above them bigger.** `02-generic/reset.css`:79–92 sets `margin: 0` on `body, h1..h6, p, figure, blockquote, dl, dd` in `layer(generic)` (the declaration itself is at :91); `layer(legacy)` is below it, so the *class* rules that carried the spacing lose. Measured on `slo.html` in headless Edge 150, element-aligned, before → after: `.card .hint` ×3 `0 0 16px` → **`0`** (16px of breathing room under every card heading, gone), `.card h2` ×3 `0 0 4px` → `0`, `.page-head p` ×1 `5px 0 0` → `0`. In the same pass `03-elements/typography.css`:66–77 takes `.page-head h1` **22px → 24px** (tracking `normal` → `-0.2px`) and `.card h2` ×3 **15px → 18px**. Net: headings ~20% bigger with zero gap beneath them. Card 1 `158px` → `143.05px`, card 2 `224px` → `214.05px`, `.page-head` `52px` → `45.95px`. Also in this bucket, harmless: `forms.css`:**101–102**'s `font-family: inherit` takes the two `<button class="btn-ghost">` off **Arial** onto IBM Plex Sans (the `<a>` was already Plex — anchors inherit, buttons do not; note `:58` carries the same declaration but its rule is `input`/`select`/`textarea` and does **not** match a button — this row's first draft cited `:58`/`:102` and both were wrong), `:63`'s `--radius-control` takes the five `select`/`input` 9px → 11px with `:62` shifting their border to `--color-border`, and `typography.css`:97-103's `code, kbd, samp, pre` takes five inline `code` 12.5px → 13.5px onto `"IBM Plex Mono", ui-monospace, monospace` — **D23's first live appearance**, since no `@font-face` declares that family and no woff2 in the repo provides it. **This is D21 firing on a real page for the first time** — the row that predicted a reset in this tree would not be neutral, written before any page loaded `main.css` | UI-031 2026-07-31, in the browser | the reset is correct and must not be narrowed to win the spacing back: `layer(generic)` zeroing margins is the whole premise of the new tree, and the spacing is supposed to come from the components that replace those legacy classes. Restoring it inside UI-031 means either authoring `.card`/`.pagehead` components in a task scoped to one page's `<link>` block (§12.2), or editing `99-legacy/slo.css`, which is append-never. UI-021 already narrowed this reset once (the universal `padding: 0` was not ported, D21) and narrowing it again to protect legacy class margins would make the reset useless on the pages it is actually for | **UI-040** (`card` + `page-head` → `.card`/`pagehead`, which is where the padding and heading spacing land) with **UI-041** for the buttons. Until then every page UI-031/UI-032 migrates ships with tighter cards. **Flagged to Irfan at handover, not shipped silently** — it is visible, it affects all nine pages, and no ratchet metric sees it |
| D29 | **Two different files are both named `theme.css`, and the browser's Network panel shows only the basename — it cost a real false alarm on the first browser check of the epic.** `static/theme.css` (13787 bytes) is the OLD live stylesheet — `--fg`, `--brand`, `--app-bg`, `--sidebar-bg`, `--line`, `--panel` — still linked by the six unmigrated pages and deleted in UI-064. `static/css/01-settings/theme.css` (8682 bytes) is the NEW Tier 2 semantic-token file authored in UI-020 — `--color-canvas`, `--color-surface`, `--color-text` — and `main.css`:87 imports it into `layer(settings)`. On a migrated page the first is absent and the second **must** load; removing it would take the whole Modern palette with it. But Chrome/Edge list both as plain `theme.css` in the Name column, so `slo.html` reads at a glance as though the link this task removed is still there. Irfan hit exactly that at the UI-031 handover, checked it rather than assuming, and was right to: initiator `main.css:87` is genuinely a `theme.css` import — of the other file. **Telling them apart in the browser:** hover the row for the full path, or read the size — **8.7 kB is the new one, 13.8 kB the old** — those are content sizes; Chrome's Size column shows *transferred* on the top line with content beneath, so the panel may read ~9.0 kB and ~14.1 kB instead (Irfan saw 9.0). Either pair discriminates: 8.7-or-9.0 cannot be confused with 13.8-or-14.1. The two claims that look contradictory are both true at once: `static/theme.css`'s token names all resolve `<EMPTY>` on a migrated page, while `01-settings/theme.css`'s are populated. `01-settings/theme.css`'s own header already warns *"NOT the same file as static/theme.css"*, which was written for a session reading source — it does not help someone reading a Network panel | UI-031 2026-08-01, at Irfan's browser check | UI-031 owns one page's `<link>` block and the entry-file mechanism. The clean fix is renaming `01-settings/theme.css` (`roles.css` or `semantic.css` — it holds semantic roles, and the ITCSS layer already carries the word *settings*), which touches a reviewed Sprint-2 file plus `main.css`'s import and is a naming decision for Irfan, not a defect to patch inside a page migration (§12.2). Doing nothing is also defensible: the collision dies on its own in UI-064 when `static/theme.css` is deleted | **UI-064** resolves it by deletion with no work at all — but the ambiguity is live for the whole of UI-031's remaining four pages and all four of UI-032's, i.e. every browser check between here and Sprint 6. If Irfan wants it gone sooner, a rename is a small standalone task: one file, one `@import` line in `main.css`, no page touched, no ratchet movement. **Until then, quote full paths in every handover click-list** — `/static/css/01-settings/theme.css` vs `/static/theme.css`, never bare `theme.css`. That wording is what caused this |
| D30 | **"The N unmigrated pages" is written in three places and is wrong in all three — the same conflation each time, and always in the sentence that hands `theme.css` over to the next session.** The number quoted is the count of pages that still **link** `/static/theme.css`; the noun attached to it says **unmigrated**. Those are different sets, because `landing.html` and `print.html` are unmigrated and never carried `theme.css` at all — which is **D9**, already on this board. Measured at each point in history rather than argued: at UI-031a `pages/slo.css`:38 says "six unmigrated pages" — 6 linked it, **8** were unmigrated; at UI-031b `pages/slo-health.css`:35 says "the five unmigrated pages" — 5 linked it, **7** were unmigrated; and **D29 itself**, two rows up, says "still linked by the six unmigrated pages" with the same split. The count is right every time and the noun is wrong every time, so nothing downstream has broken yet — but the sentence exists to tell a future session which pages still need migrating, and it gives the wrong list to anyone who trusts the words over the numbers. This is the **third** appearance of the epic's most-repeated failure shape (a number carried instead of counted): UI-020 was FAILED for "linked by 8 pages — it is 7", UI-030 for "the mockup's nav is 12 items — it is 11" | UI-031b 2026-08-01, by the review agent, while it was checking the *fourth* instance in `pages/library.css` — that one was fixed in-task because the task owned the file | The three stale instances sit in files owned by **completed, reviewed and committed tasks** (`pages/slo.css` UI-031a, `pages/slo-health.css` UI-031b, `DEFERRED.md` D29). §12.2: editing them from inside the library migration is a drive-by fix that makes this diff unreviewable, and rewriting a prior task's reviewed record is exactly what §12 exists to prevent. It is prose-only — no CSS, no metric, no page behaviour, so nothing is at risk while it waits | A single docs-only sweep, cheapest as its own small task in the UI-013a / UI-017a / UI-018a mould (they set the precedent that a self-contained correction gets its own reviewed commit). Or fold it into **UI-064**, which deletes `/static/theme.css` and makes all three sentences moot. Whoever takes it: give the count *and* name the pages, e.g. "still linked by index, taqseem, blueprint and bank — four of the six unmigrated pages", so the two sets can never be silently merged again. **UPDATE 2026-08-04 (UI-032, `bank`): both counts moved again and the stale sentences drifted further.** Measured, not argued: **3** pages still link `/static/theme.css` (`blueprint`, `index`, `taqseem`) and **5** are unmigrated (`blueprint`, `index`, `taqseem`, `landing`, `print`) — the gap is now two, not one, because `landing` and `print` never linked it (D9). So `DEFERRED.md`'s own D29 ("still linked by **the six unmigrated pages**"), `static/css/main.css`:10-12 ("the other eight pages still carry their own `app.css` + `theme.css`") and `main.css`:117 ("still a plain `<link>` on 6 pages") are each wrong in the count *as well as* the noun now. Still **not** fixed here (§12.2 — three files owned by committed tasks), but recorded rather than left to be discovered again. `pages/bank.css` was checked and does **not** reintroduce the phrasing |
| D31 | **`--color-text-muted` crosses WCAG AA on a TINTED surface: two `<label>`s in `.urdu-toggle-row` land at 4.44:1 — the first place the label token actually fails, not merely falls.** Same mechanism as D26, third page in a row: `03-elements/forms.css`:47's bare `label { font-size: var(--text-label); font-weight: var(--font-weight-strong); color: var(--color-text-muted) }` is an ELEMENT rule in `layer(elements)`, so it outranks `99-legacy/bank.css`:108's `.urdu-toggle-row label { margin: 0; font-size: 13px; font-weight: 600; color: var(--ink); cursor: pointer }` — a *class* rule in `layer(legacy)`, and more specific, which is exactly D21's point that layer order is decided before specificity. (The rule is quoted in full deliberately: its `font-weight: 600` is what makes the failing labels 12px/**600**, which is why the large-text exemption is discussed below at all. An abridged first draft of this row dropped it.) Measured on `bank.html` in headless Edge 151, both states, with the backdrop found by **walking the ancestor chain to the first non-transparent background rather than assumed** — D26's first draft failed for assuming one. **Method limit, stated so the next page does not inherit a wrong number:** the walk stops at the first background with alpha > 0 and does **not** composite translucent layers. Both backdrops here are fully opaque (`rgb(255,255,255)` and `rgb(245,247,251)`), so the figures are exact on this page — but pointed at a translucent surface, such as the `rgba(255,255,255,.07)` hover states `99-legacy/bank.css` paints in the sidebar, it would return the wrong colour. Composite before reusing the method, particularly on `index`. All 59 `<label>`s on this page take the token; **57 of them sit on `#ffffff` and land at 4.76:1, which PASSES** (before: 15.59:1 / 16.00:1). The two that fail sit on `.urdu-toggle-row`'s own `background: #F5F7FB` (`99-legacy/bank.css`:102): before `rgb(22,33,58)` on `rgb(245,247,251)` = **14.92:1**, after `rgb(100,116,139)` on the same = **4.44:1**, against AA's 4.5:1 for 12px/600 (the large-text exemption needs ≥18.66px bold or ≥24px, so it does not apply). The shortfall is 0.06 and it is still a fail. **The pattern this establishes is worth more than the two elements**: slate-500 was chosen against the white canvas and clears AA there with ~0.26 to spare, so *any* legacy surface tinted even slightly off-white takes the token below the line — `library`'s 44 labels cleared at 4.76:1 only because they are all on white. Every remaining page with a tinted panel will hit this | UI-032 `bank` 2026-08-04, in the browser | §12.2 — this task's scope is one page's `<link>` block plus the entry file. All three fixes belong to someone else: a darker muted role or a surface-aware token is `01-settings` (Sprint 2, closed); narrowing `forms.css`'s `label` so it sets size and weight but not colour edits a reviewed UI-021 file and changes all nine pages; and the right fix — a `.field` that owns its own label colour against the surface it sits on — is the field component. Patching `99-legacy/bank.css` is barred: that file is append-never | **UI-042** (`field` component), with D26/D27 as the same family — a component in `layer(components)` outranks the element rule and can pick its colour per surface. If Irfan wants AA before Sprint 4 lands, the one-line version is a `--color-text-muted-strong` role used by `forms.css`'s `label`, which would lift all 59 at once. **Flagged at handover, not shipped silently** |
| D32 | **The orphan-TOKEN check reads stylesheets and never reads the markup, so a `var()` inside an inline `style=""` is invisible to it — and `blueprint` turns out to read NINE such tokens with no fallback.** `scripts/css_orphans.py` collects what `99-legacy/<page>.css` declares and reads; a token read from a `style="…var(--x)…"` attribute in the page's HTML is a read the script cannot see, so a page can measure 0 orphan tokens and still lose a value when `/static/theme.css` is unlinked. Found on `bank`, whose markup reads `--line` at `static/bank.html`:98 and :593 — a name `99-legacy/bank.css` declares zero times — and measured live: 2 elements go `rgb(234,237,243)` → `rgb(226,232,240)`, i.e. down to the inline fallback. Harmless there **because** the fallback exists, which is the shape D9 records for `print`'s `--accent`; it is not harmless by luck of the stylesheet check. **The whole project was then swept rather than the one page argued about** — every page's inline `var()` reads, minus what its own legacy file declares, minus what the new tree declares: `slo`, `slo-health`, `library` **none — nothing already shipped is affected**; `bank` `--line` only, with a fallback; `index` **none**, so its 224 inline `style=""` attrs (the project's largest share) are a volume risk and not an exposure one — every name it reads inline is declared in `index.css`'s own `:root`; `taqseem`, `landing`, `print` none. **`blueprint` is the outlier and it is not close**: its markup reads `--accent`, `--accent-soft`, `--brand`, `--chip-bg`, `--fg`, `--green`, `--green-bg`, `--muted` and `--red` from inline styles, all `/static/theme.css`-only, and **not one of the nine carries a fallback**. So `blueprint`'s real exposure is 21 orphan tokens *plus* 20 orphan rules *plus* 9 unsupplied markup reads — the stylesheet-only figure understated it, and the page being HELD (2026-08-04) is the more obviously right call for it | UI-032 `bank` 2026-08-04, by the review agent, checking whether the entry file's "orphans no token" claim was true of the page or only of its stylesheet | §12.2 — this task owns one page's `<link>` block. Teaching `css_orphans.py` to parse markup is a change to a script two committed tasks depend on, and it would move published numbers, so it needs its own reviewed commit in the UI-017a / UI-018a mould. Nothing is at risk meanwhile: the sweep above is the answer for all nine pages as they stand today, and the only page it changes the picture for is already held | A small script task — add an inline-`style` pass to `css_orphans.py --names`, reporting markup reads as their own column so the two sources never merge, then re-run the nine-page table. Cheapest **before `blueprint` is unheld in Sprint 4**, since that is the page whose number moves. Whoever takes it: report fallback / no-fallback separately, because that distinction is the whole difference between `bank` (fine) and `blueprint` (nine bare reads) |
| D33 | **`print.html`'s Urdu letterhead line will overlap the moment the page is migrated AND a school fills in its Urdu name — the same mechanism `bank` is HELD for, on the Ctrl+P page.** `99-legacy/print.css`:126 `.letterhead .school-ur` sets `"Noto Nastaliq Urdu"` at 19px and **no `line-height`**, and no ancestor supplies one either — `body`:24, `.sheet`:112 and `.letterhead`:118 were each read and none declares it. So it inherits, and `03-elements/typography.css`:41's `body { line-height: var(--leading-body) }` sits in `layer(elements)` and beats `layer(legacy)`. Measured in **print media with the webfont loaded first**: `normal` → **27.55px while the glyph line rect stays 47px**, i.e. a 47px ink extent inside a 28px box. **Nothing is broken today on two counts**, and both must hold for that to stay true: `print.html` is at HEAD and unmigrated, **and** `school_name_ur` is empty in this database. **Either one changing exposes it** — and the migration is already measured, approved on sight and parked (`docs/ui/parked-print.css`), so the likelier trigger is shipping that. Note what this is NOT: the Urdu verified in Irfan's print preview (`Name / نام:`, `تمام سوالات کے جواب دیں۔`) is a **system naskh fallback inside `.label`/`.instructions`**, both computing to `"IBM Plex Sans", system-ui, sans-serif` — 16px line box against 30px for the same string forced into Nastaliq — so it fits a 1.45 leading and is unaffected. `.school-ur` is the page's only genuinely-Nastaliq element, and `print.css`'s five `!important` declarations are all `display: none` and protect nothing here | UI-032 `print` 2026-08-05, measured while checking whether `bank`'s hold applied to this page | §12.2 — the task owned one page's `<link>` block, and this needs a **decision about Nastaliq leading**, not a one-line patch. It is the same decision `bank` is held for, so patching it here in isolation would pre-empt that and probably diverge from it. `99-legacy/print.css` is also append-never | **Whichever task takes `bank`'s Nastaliq decision must take this with it — they are one problem in two places.** That task does not exist yet; see **D34**. **Verify by filling `school_name_ur` and printing** — no automated check in this epic reports it |
| D34 | **Three of the five HELD pages need work that no task ID owns, and following `PLAN.md`'s Sprint 4 as written would end the sprint with them still held.** `PLAN.md`:195-198 gives Sprint 4 as **UI-040** card + pagehead, **UI-041** button + chip/tag/badge, **UI-042** modal + field, **UI-043** tables + status-bar + domain. Matched against what each held page is actually waiting for: `taqseem` needs UI-040 + UI-041 ✅ and `index` needs UI-041 plus a `.main` layout fix ⚠️, but **`blueprint` needs nav/shell components, `bank` needs the Nastaliq leading decision, and `landing` needs a display/hero type step — and none of the four task IDs covers any of the three.** `nav.css` is listed in `PLAN.md`:80's `05-components/` sketch but appears in **no** task row; type and leading were UI-021's (Sprint 2, closed) and Sprint 4 never revisits them. This is not a new discovery in parts — STATUS.md already records that landing "needs either an ID of its own or an explicit home", and UI-030's own notes hand the nav control to "a Sprint 4 component" that the table does not contain — **but it has never been counted across all five pages at once, and counted, it is three of five** | UI-032 wrap-up 2026-08-05, reading `PLAN.md`'s Sprint 4 table against the five held pages rather than against duplication counts | §12 rule 2 and rule 11 — a session does not edit another planning doc in passing, and this is a **scope decision for Irfan**, not a documentation tidy. Getting it wrong by guessing would put three pages' fate inside tasks scoped for something else | **A short planning task, before UI-040 starts.** Give the three orphans IDs (or an explicit home inside UI-040…043) and settle Sprint 4's order. Recommended order is by **how many pages each unblocks**, not by duplication count: UI-041 first (opens `taqseem`, `index`, part of `blueprint`), then UI-040, then nav (opens `blueprint`, and D26's sub-AA sidebar subtitle), then type/leading (opens `landing`, `bank`, and D33). Note that **D32 is cheapest before `blueprint` is unheld**, since that is the page whose exposure number it moves |
| D35 | **A space-token regression was REPORTED on `print` and this session could not reproduce it from the repo — recorded as an open lead, not as a fact, because three of its four checkable parts do not hold against `feat/ui-architecture`.** The report: migrating `print` moves a right margin **48px → 24px** and collapses a `.count-grid` on Q10, the cause being that the `--space-*` scale in `layer(settings)` **loses** to a legacy unlayered `:root`, i.e. a space-flavoured D31. **What was checked in the repo, and what it says.** (1) **`.count-grid` does not exist** — `count-grid` / `countGrid` / `count_grid`, case-insensitive, across the whole repo excluding `.git` and `node_modules`: **zero matches**, in CSS, HTML and JS alike. (2) **No legacy file reads `--space-*`** — `grep -- "--space-" static/css/99-legacy/`: **zero matches** in all nine files, `print.css` included. The scale is declared only in `01-settings/tokens.css`:139-142 and `01-settings/theme.css`:117-120 and consumed only by the new tree (`03-elements/tables.css`:47,58 and `04-objects/shell.css`), so on these pages legacy and the new tree do not contend over it at all. (3) **The stated layer direction is inverted** — `static/css/main.css`:42 declares `@layer legacy, settings, generic, elements, objects, components, utilities`, in which `legacy` is **first and therefore weakest** and `settings` outranks it; and after migration the legacy file arrives via `@import … layer(legacy)` rather than unlayered, so the unlayered-`:root` premise describes the page **before** the swap, not after. (4) The paper was cited as **`id=121`**, but this app's paper IDs are UUIDs and the parameter is `paper_id` — `?id=` renders a blank page, which `NEXT-SESSION.md` records as having cost time twice. **What this session DID measure, in print media, on three real papers, drift 0** (`0d04c750` 20Q, `a5015cda` 20Q/19 images, `9ade2655` 25Q/25 images, Edge 151 at 1280×900, `document.fonts.ready` awaited): `@page { size: a4; margin: 0px }` present and unlayered; `html`, `body` and `.print-main` each padding/margin/border **0 0 0 0**; **`.sheet` padding 52.9134px on all four sides = exactly 14mm**, the single source `print.css`:1-2 claims; the three knobs resolving `14mm` / `14px` / `14px` off `documentElement.style`. **There is no 48px or 24px anywhere on the margin chain** — `.print-main`'s 24px is screen-only and `print.css`:219 zeroes it in print. **This is the BEFORE state only: the swap was never performed, so no AFTER exists and nothing here refutes the report on its own terms.** The two are not yet comparable, and that is the honest position | reported by Irfan 2026-08-05; repo checks and the BEFORE measurement taken the same day | `print` is HELD on Irfan's call, so nothing is shipping either way, and the mechanism as stated would be a **Sprint 4 cascade fix** rather than this task's. Writing it onto the board as established would make it the next session's premise, which is this epic's most-repeated failure shape — so it is written as a lead with its evidence attached | **Reproduce or retire it before Sprint 4 plans around it.** Cheapest path: perform the one-line swap, measure `.sheet` and every padding delta **by element name** in print media, and diff against this row's BEFORE numbers — the harness for exactly that was built this session and the method is in `NEXT-SESSION.md`. If a 48→24px move is real it is on some element the margin chain does not contain, and naming that element is the whole answer. If `.count-grid` is real it is not in this repo, which would mean the observation came from another branch, machine or page — worth settling first, since that single fact decides whether any of the rest applies here |

---

## Resolved

| ID | What | Resolved by |
|---|---|---|
| D1 | `@layer` adoption pending a browser check | **Adopted 2026-07-28.** Floor is Edge/Chrome 99 (Mar 2022) and Edge auto-updates, so the deployment clears it by years. Reversible in one line if anything ever renders unstyled. |
| D6 | `config/brand.json` `primary`/`navy` don't match the Modern palette | **Done in UI-003** (`2eba2f8`). Deleted rather than reconciled — nothing consumed them, re-verified at implementation: `brand.js` reads only `full_name`/`tagline`, no Python indexes the keys, no test touches the endpoint. The `--navy` declarations in the pages are local CSS vars, not reads of `brand.json`. |
