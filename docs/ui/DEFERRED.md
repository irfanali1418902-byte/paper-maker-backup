# UI-ARCH — Deferred / Parking Lot

Anything found mid-task that is **out of scope for that task** lands here instead of being
fixed inline. This file is the pressure-release valve that makes the "one task, one page,
no drive-by fixes" rule survivable.

**Rule: if you find it and it is not your task, write it here and move on.**
Do not fix it. Do not "just quickly" fix it. Adding a row here is the correct outcome.

Format: `| ID | What | Found in | Why deferred | Proposed home |`

---

## Open

| ID | What | Found in | Why deferred | Proposed home |
|---|---|---|---|---|
| ~~D60~~ ✅ | **BAND 2026-09-03 — MERGE HO GAYA, AUR ROW KI APNI SHART POORI HUI** ("Row tab band karo jab merge ho jaye, pehle nahi"). `fix/persist-batch-learning-outcome` (`f547f21`) pehle `master` mein fast-forward kiya gaya, phir epic us ke OOPER merge hua (`dc9436a`) — is tarteeb se, taake wo ek line epic ke saath ek hi tree mein aaye. **Naapa gaya merge ke baad:** `question_service.py`:65 par ab `"learning_outcome": req.learning_outcome` mojood hai (pehle sirf :30 prompt ke liye aur :106 manual ke liye thay — wohi surakh tha), aur `tests/test_persist_batch.py` ke **5 tests** merged tree par pass hain; kul **1078 → 1083**. Conflict ka imkan pehle hi naapa gaya tha: epic ne `question_service.py` aur `test_persist_batch.py` chhui hi nahi thin. **⚠ Jo ab bhi khula hai aur ye row us ka zimma nahi leti:** bank ke **130** sawal ab bhi khaali `learning_outcome` rakhte hain — wo bulk Excel import ke hain, `syllabus_topic_id` NULL, aur unhein syllabus se bhara ja hi nahi sakta. Un ka dohraav wala masla `D61` mein hai. **Aur ek baat jo backfill ne sikhayi: in topics ka apna `learning_outcome` akser TITLE HI HOTA HAI** (baqi PY2 ke 18 mein 18/18, PY1 ke 10 mein 10/10), yani bhara hua khana asal maloomat nahi — wo syllabus ke data ki kharabi hai, is bug ki nahi, aur ye merge us ko theek nahi karta. ~~Original row:~~ **`persist_batch()` ne `learning_outcome` kabhi save nahi kiya — 571 sawal khaali, aur FIX IS BRANCH PAR NAHI HAI.** `app/services/question_service.py`'s `persist_batch()` insert-dict banata hai aur us mein `learning_outcome` daalta nahi, halanke wohi function `:30` par `req.learning_outcome` ko prompt ke liye parhta hai. `app/repositories/questions_repository.py`:45 use `question_row.get("learning_outcome")` se uthata hai — **bina default ke, jab ke usi ke ooper `:44` par `get("source", "gemini")` hai** — is liye ghair-mojood key par na error aata hai na koi test toota: khamoshi se `NULL`. **Naapa 2026-09-02:** `source='gemini'` ke **571 mein se 571** khaali (batches 08-21, 08-22, 08-23, 08-26, 08-31, 09-01). **Ye sirf seeding ka raasta nahi:** `app/api/questions.py`:61 bhi `persist_batch` ko bulata hai, yani `/api/generate` se bana har sawal bhi khaali aata tha — aur us raaste par koi backfill nahi chalti. `save_manual_question()` (`:105`) ye khana theek bharta hai, aur usi ka mojood hona batata hai ke ye bhoolna tha, faisla nahi. **Kisi gate ne is liye nahi pakra ke `persist_batch` par ek bhi test nahi tha** (`git grep persist_batch -- tests/` khali). **DO ADAD JO BOARD SE GHALAT CHALE AUR YAHAN THEEK KIYE JA RAHE HAIN:** `STATUS.md` kehta hai "**701** khaali" aur "har seed batch **07-30** se 09-01" — 701 bank ke kul khaali sawal hain, is bug ke nahi, aur **07-30 seed batch hai hi nahi**: wo 130 rows `Happy Birds - English Pre Year 1` ka bulk Excel import hain (`source='manual'`, `syllabus_topic_id` NULL), aur `bulk_import_service.py`:334 ye khana **bharta** hai — wo is liye khaali hain ke Excel ka column khaali tha. **Ye fix un 130 mein se ek bhi nahi badlegi.** Wahi purani bimari: row ka adad naapa nahi gaya | Seeding session 2026-09-01 ne khaali khana dekha (board par darj); wajah **2026-09-02** ko dhoondi gayi, aur board ke dono adad usi din naapne par ghalat nikle | **`PLAN.md` §6 is epic par `service/repository` tabdeeli mana karta hai** ("Backend findings go to `DEFERRED.md`") — is liye fix yahan nahi ki gayi. Bandish mutlaq nahi hai (D40 ne 2026-08-21 ko Irfan ki manzoori se `app/schemas/requests.py` chhua tha), magar Irfan ne 2026-09-02 ko saaf `master`-wala raasta chuna taake epic ka diff frontend hi rahe | **HO CHUKA 2026-09-02, MAGAR DOOSRI BRANCH PAR:** `fix/persist-batch-learning-outcome` (`master` se kaati, commit `f547f21`) — ek line + paanch tests, **980 pass, ruff saaf**, control se sabit (fix se pehle wali file par 3 fail). **IS BRANCH PAR WO LINE NAHI HAI**, is liye `feat/ui-architecture` se chalaya gaya `seed_bank.py` ab bhi khaali `learning_outcome` likhega. **Do natije:** (1) aaj ki baqi seeding ke baad **backfill zaroori hai** — 571 rows syllabus se seedhe SQL `UPDATE` se bhar sakti hain (topic ka `learning_outcome` 571/571 bhara hai), **koi AI call nahi**, aur wo backfill naye rows ko bhi utha legi; (2) `master` merge ke waqt ye line epic mein aa jayegi — us waqt `persist_batch` par tests bhi saath aayenge. **Row tab band karo jab merge ho jaye, pehle nahi** |
| D65 ⚠ | **`label` aur `html, body { height: 100% }` — dono `agree` mein hain, dono koshish ki gayi, aur DONO NE 347 DELTAS DIYE. Ye row us naakami ka record hai taake koi dobara na aazmaye.** 2026-09-04 ko UI-082 mein `label { display: block; margin-top: 14px }` + `label:first-of-type { margin-top: 0 }` chaar legacy files se `03-elements/forms.css` mein le jaye gaye aur `html, body { margin: 0; height: 100% }` chaar files se delete kiya gaya. Gate ne **347 element × property deltas** diye aur sab kuch revert kar diya gaya (revert ke baad 0 deltas, tasdeeq-shuda). **DO ALAG SABAB, DONO NAAPE GAYE:** **(a) `label` LAYER UPAR JATE HI HAR PAGE-LEVEL OVERRIDE KO HARATA HAI.** Diff mein `label  display: flex → block` aur `margin-top: 0px → 14px` bar bar aaya. Is app ke compound label overrides (`.urdu-toggle-row label`, `.radio-row label` waghaira) `layer(legacy)` mein hain; `layer(elements)` un sab ko specificity se qat-e-nazar harata hai. **Ye bilkul D51 wali shakl hai** — wahan bhi teen properties layer upar jate hi chhe pages tor deti thin. `print` par bhi asar pada (10 deltas), jahan `label` ki rule thi hi nahi. **(b) `height: 100%` INERT NAHI HAI.** Delete karne par ek page ka `body height` **900px se 72666.2px** ho gaya. Us se pehle wali naap — "jin chaar pages par ye rule hai un ka body height 805px, aur jin par nahi un ka bhi 805px" — **sahi thi magar kaafi nahi**: wo ek viewport, ek content-halat ka naap tha, aur `height: 100%` ka asar tab khulta hai jab andar ka content lamba ho jaye | UI-082, 2026-09-04, `agree` ka teesra batch. **Aur yahi is poore din ka sab se saaf sabaq hai: `agree` ka matlab sirf itna hai ke rule ki NAQAL har file mein ek jaisi hai. Us se ye bilkul nahi nikalta ke rule ko layer upar le jana mehfooz hai** — kyunke layer upar jate hi wo un rules ko bhi harane lagti hai jo aaj us se jeet rahi hain. Ek hi din mein `agree` ke chaar members isi bunyaad par ruke: `.row` (D63), `.app-nav` (D64), aur ab `label` + `html, body` | **`label` ke liye D51 ka hi raasta hai:** pehle un compound overrides ko ek class par lao ya unhein bhi upar uthao, phir bare `label` ja sakta hai. Ye markup ka kaam hai, CSS ka nahi — aur D51 wahi baat `.row`/`.field-row`/`.filter-row`/`.ps-row` ke liye pehle se kehti hai. **`html, body { height: 100% }` ke liye:** `02-generic/reset.css` ka header khud kehta hai ke us ka ghar `04-objects/shell.css` hai (UI-030 ka faisla) — magar wahan le jane ka matlab hai use das ke das pages par lagana, aur `print` ka body viewport tak doosre raaste se pahunchta hai (`display: flex; min-height: 100vh`), jo usi header mein likha hai | **Dono ko chhoro jab tak D51 ka markup wala kaam na ho.** Jo bhi inhein dobara uthaye: pehle `css_drain_probe.mjs` chalao (wo yehi sawal poochta hai — "ye rule delete karne se kuch hilta hai?") aur DONO widths par, kyunke `height: 100%` ka asar lambe content par khulta hai, khali page par nahi |
| D64 | **`.app-nav` `agree` mein hai magar component banane se `index` aur `print` par 10px padding aa jayega.** Paanch legacy files (`bank`, `library`, `slo`, `slo-health`, `taqseem`) mein `.app-nav { display: flex; flex-direction: column; gap: 2px; padding: 0 10px }` byte-identical hai. **Magar `index` aur `print` par bhi `.app-nav` ka markup hai aur un ke paas ye rule NAHI hai.** Naapa gaya 2026-09-04 (`css_selector_probe`, saat pages): un dono par `display: flex`, `flex-direction: column`, `row-gap: 2px` to compute hote hain (wo `05-components/nav.css` se aate hain) **magar `padding-left` 0px hai, jab ke baqi paanch par 10px.** Component `padding: 0 10px` ke saath aaya to **do zinda pages par nav ka padding badal jayega** | UI-081, 2026-09-04, `agree` ka doosra batch uthate waqt. **Ye D63 ke bilkul saath wali bimari hai aur dono ek hi sabab se nikleen:** audit ka `agree` sirf un FILES ko dekhta hai jin mein rule mojood hai, un PAGES ko nahi jin par MARKUP hai magar rule nahi. Do rows ek hi din mein isi sawal se bane | **Faisla chahiye, kaam nahi.** Sawal ek hi hai: kya `index` aur `print` ke nav ko bhi 10px padding milna chahiye? Agar haan, to component seedha ja sakta hai aur wo do pages jaan-boojh kar badlenge (chhota, magar DIKHNE WALA farq). Agar nahi, to `padding` ko component se bahar rakhna paregi aur paanch pages ko wo apni file mein deni paregi — jis se faida bahut kam ho jata hai | Irfan se ek sawal. **Us se pehle browser mein dono nav saath dekh lena chahiye** — 10px ka farq nazar aata hai ya nahi, ye khud ek naap hai (wahi tareeqa jo D62 ke liye likha hai) |
| D63 | **`.row` `agree` mein hai magar component banane se `index` ka `flex-wrap` badal jayega — aur wajah ye hai ke audit ka `agree` wo sawal poochta hi nahi jo ahem hai.** `slo`, `slo-health` aur `taqseem` ki legacy files mein `.row { display: flex; flex-wrap: wrap; gap: 10px; align-items: center }` byte-identical hai, is liye audit ise `agree` (= "koi faisla darkar nahi") kehta hai. **Magar `index` par bhi `.row` ka markup hai, aur us ka apna rule `pages/index.css`:281 mein hai jo sirf `display`/`align-items`/`gap` declare karta hai — `flex-wrap` NAHI.** Naapa gaya 2026-09-04 (`css_selector_probe`, chaar pages): `index` par `flex-wrap` **`nowrap`** compute hota hai (yani initial value, kisi declaration se nahi), baqi teen par **`wrap`**. Agar `.row` component `layer(components)` mein `flex-wrap: wrap` ke saath aaya to `index` ka rule us ko override nahi karega (wo property declare hi nahi karta) aur **`index` ka `.row` `nowrap` se `wrap` ho jayega — ek zinda page par nazar aane wali tabdeeli.** `pages/index.css`:262 ka comment isi khatre ko pehle se darj karta hai ("a layer(components) rule would repaint all seven — measured, 2 deltas on taqseem alone") | UI-079, 2026-09-04, `agree` families ka pehla batch uthate waqt. **Yehi cheez `agree` label ki hadd batati hai: wo sirf un FILES ko dekhta hai jin mein rule mojood hai, un PAGES ko nahi jin par MARKUP hai magar rule nahi.** Isi liye is commit mein har selector par das ke das pages par `css_selector_probe` chalaya gaya, aur `.slo-main`/`.empty-state` saaf nikle (sirf do pages par match) jab ke `.row` nahi | **Ye ek line ka kaam hai magar ek faisla maangta hai.** Mehfooz raasta: `pages/index.css`:281 mein `flex-wrap: nowrap` SAAF likh do (aaj ki computed qadr bilkul wohi hai, is liye 0 deltas), phir component `wrap` ke saath aa sakta hai. Us ke baghair `.row` ko chhorna hi theek hai. Ye "drive-by" nahi hai — `index` ki file chhoona is task ka hissa nahi tha | Agle `agree` batch ke saath. Pehle `flex-wrap: nowrap` wali ek line `pages/index.css` mein, gate chalao (0 deltas aane chahiyen), phir `.row` component banao aur dobara gate. **Do alag qadam, do alag naap — ek saath karne par pata nahi chalega ke kaun sa qadam kis delta ka zimmedar hai** |
| D62 | **`index` par ek hi tagline do jagah likha hai aur wo do alag tarah paint hota hai.** `index.html`:16 aur `:50` par bilkul ek jaisa markup hai — `<div class="tag" data-i18n="brand.tag">Phase 1 · MVP</div>` — magar pehla `.brand` ke andar hai aur doosra nahi. Naapa gaya 2026-09-03 (dono elements, 1280px): **`.brand` wale ka `letter-spacing` 0.44px hai, doosre ka `normal`**; baqi sab barabar (font-size 11px, colour `rgb(79,70,229)`). Wajah wo aakhri zinda legacy declaration hai jo `.brand .tag` par thi aur ab `pages/index.css` mein hai — us ka selector `.brand` maangta hai, aur doosra tagline `.brand` ke bahar hai. **Ye aaj ka bug nahi aur UI-078 ne is ko banaya nahi — sirf naapte waqt nazar aaya**, kyunke us se pehle kisi ne dono `.tag` elements ko saath naapa hi nahi tha | UI-078, 2026-09-03, `.brand .tag` ko legacy se `pages/index.css` le jate waqt — us move ne 0 deltas diye, yani ye farq pehle se mojood tha | **Ye dikhne wali tabdeeli hai, drain nahi.** Dono ko barabar karne ka matlab hai ya to tracking doosre par bhi lagana ya pehle se hatana — dono soorton mein ek tagline ki shakl badlegi, aur wo Irfan ka faisla hai, mera nahi. `CLAUDE.md` §12.2 isi ko drive-by kehta hai | Ek chhota faisla: **kya dono taglines ko ek jaisa hona chahiye?** Agar haan, to `pages/index.css` ki `.tag` rule mein `letter-spacing` daal do aur `.brand .tag` khatam — ek line ka kaam, magar `index` ke sidebar YA topbar mein se ek ki shakl badlegi. Agar nahi, to `.brand .tag` waise hi rahe aur ye row band. **Faisle se pehle browser mein dono ko saath dekh lena chahiye** — 0.44px ka farq screenshot par nazar aata hai ya nahi, ye khud ek naap hai |
| D61 | **Bulk Excel import ke 130 rows apne andar dohraav rakhte hain — saare 130 kisi na kisi dohre group mein hain.** Naapa 2026-09-03 ke audit mein: poore bank mein `lower(trim(question_en))` par **78 dohre groups** hain, aur un mein shamil rows ka grade-wise hisaab — **`(topic NULL)` 130**, PY1 13, PY3 10, PY2 10. Yani `syllabus_topic_id` NULL wale **130 ke 130** English bulk-import rows dohre hain, jab ke AI-seeded grades ka hissa 33 hai. Source ke hisaab se: `manual` 143, `gemini` 20. **Ye aaj ka bug nahi aur seeding se nahi aaya** — ye wahi `Happy Birds - English Pre Year 1` import hai (`source='manual'`, `bulk_import_service.py`) jo D60 mein bhi zikr hua. Isi import ka `learning_outcome` khana bhi khaali hai aur **backfill se nahi bhar sakta** (koi syllabus row hai hi nahi), is liye ye do kharabiyan ek hi import ki hain | 2026-09-03 ka board audit — dohraav naapte waqt, jab 09-03 ki seeding ke naye 84 rows ko poore bank ke khilaf check kiya ja raha tha (naye rows ka apna natija saaf tha: andar 0 dohre, bank ke khilaf 2, aur wo 2 bhi do alag grades mein) | **Ye CSS epic ka kaam nahi aur data ka faisla hai, code ka nahi.** 130 rows delete/merge karna teacher ke bank se sawal hatana hai — wo Irfan ka faisla hai, aur us se pehle ye dekhna hoga ke dohraav asal mein Excel file mein tha ya import ne banaya. Drain ke beech mein ye karna wahi drive-by hai jise `CLAUDE.md` §12.2 mana karta hai | Apna chhota data-session, seeding khatam hone ke baad. Pehla qadam **naapna** hai, delete nahi: Excel file mein wo 130 rows dekho — agar wahan bhi dohre hain to import theek hai aur file kharab hai; agar nahi, to `bulk_import_service.py` ne banaye. **Us se pehle in par koi paper na banaya jaye**, warna ek hi sawal do baar chhap sakta hai |
| ~~D59~~ ✅ | **BAND 2026-09-02 — DONO FILES DELETE HO GAYIN, AUR IS ROW KI APNI DALIL NAAPNE PAR GHALAT NIKLI.** Ye row kehti thi: *"Deleting it is tempting and wrong: its `--rules` mode does a job nothing else does."* **Naapa gaya 2026-09-02, aur `--rules` bhi bilkul murda hai** — wo bhi `OLD_THEME` (`:89`, yani deleted `static/theme.css`) parhta hai: `static/theme.css: 0 rules probed (0 selectors)` aur **nau pages par har column 0** (`elems` 54–56 gine, `match`/`redecl`/`partial`/`orphan`/`state`/`EXPOSURE`/`media`/`drift` sab 0) — aur ye sab naapne ke liye us ne asli browser (Edg/152) bhi chalaya. Yani **dono halves murda thay, ek nahi.** Delete kiye gaye: `scripts/css_orphans.py` (822 lines) aur `scripts/css_rules_probe.mjs` (217, sirf us ke `--rules` ka DOM half — `PROBES.md`:28) = **1039 lines**. Naapa gaya ke koi cheez in par khari nahi: kisi test mein nahi, kisi script mein nahi, sirf docs ke hawale. **Rakhne ke bajaye delete kyun:** aitmad ke saath sifar chhapne wala table na-mojood table se bura hai — is row ko khud likhna para tha *"do not quote its table before then"*, aur wo tanbeeh sirf us par kaam karti hai jo pehle `DEFERRED.md` parhe. **Kisi ne is ki jagah nahi li, kyunke sawal band hua hai, muntaqil nahi:** `static/theme.css` mojood hi nahi aur koi page use link nahi karta; qareeb-tareen zinda sawal (*"koi `var()` hal hone se reh to nahi gayi?"*) **UI-072 (D14)** band kar chuka hai — poore repo mein zero. Sprint 6 ka drain sawal `css_drain_probe.mjs` hai, jo ulta sawal hai aur mutassir nahi hua. Tafseel `PROBES.md` ke "main job is over" wale block mein. ~~Original row:~~ **`scripts/css_orphans.py` measures against a file that was deleted on 2026-08-13 and says so itself.** It resolves token reads against `static/theme.css`; that file is gone, so the script opens every run with `WARNING: … not found — 'supplied' will read 0 for every page` and its `supplied` / `covered` / `compat` / `fallbk` / `collide` columns are all structurally 0. Its `decl` / `reads` / `orphan` / `dead` columns still mean something but are computed on the same now-missing baseline, so a reader taking the table at face value gets a confident wrong answer — `taqseem` reads `dead 21` there while the per-page `var()` resolution written for UI-072's audit says 12, and 12 is the one that matched reality (44 declarations removed, 0 deltas) | UI-072, 2026-08-29 | It is a measuring tool, not app code, and nothing currently depends on it — the drain uses `css_duplication_audit.py` and the probes. Deleting it is tempting and wrong: its `--rules` mode does a job nothing else does | Either retarget it at the current tree or retire it explicitly with a header saying what replaced it. Do it in whatever session next needs an orphan check — **do not quote its table before then** |
| D57 ⚠ | **NAAPA GAYA 2026-09-02: IS ROW KA SYMPTOM MOJOOD NAHI, AUR US KA TAJWEEZ-KARDA FIX NUQSAN-DEH HAI. Jo hissa sach hai us par ab ek test lag gaya hai (`tests/test_brand_hooks.py`, teen tests, control se sabit).** (1) **"sets the school name" ghalat hai.** `/api/brand` school ka naam deta hi nahi — `curl` par: `{"name":"Parcha","full_name":"Parcha Paper Maker","tagline":"Exam paper generator"}`. Wo product ka naam hai; school ka naam school-settings mein hai aur sirf chhape hue letterhead par aata hai (`print.html`:1036–37, `#schoolNameEn`/`#schoolNameUr`), **jo `brand.js` ko chhoota hi nahi**. (2) **"print's sidebar shows the hardcoded string while the other pages show the configured school name" — koi farq hai hi nahi.** `brand.js` `.brand .name` mein `full_name` likhta hai = `"Parcha Paper Maker"`, aur **wohi string pehle se har page ke markup mein likhi hai** (`slo.html`:13, `print.html`:12, `plan.html`:16). Yani **aaj daswon pages bilkul ek jaisa dikhate hain**, script pohanche ya na pohanche. (3) **Row ka rukh — "print bhi brand.js load kare" — ULTA nuqsan karega:** `print.html` ke `<body>` par `data-page` nahi hai, aur script title ko `data-page ? "<page> — <full_name>" : "<full_name>"` par set karti hai, is liye title **`Exam Paper — Print` se ghat kar sirf `Parcha Paper Maker`** reh jata — maloomat kam. **JO SACH HAI, AUR YEHI ASAL BAAT HAI:** khatra aaj ka farq nahi, **kal ki khamoshi** hai. `brand.js` presentational classes par chalta hai (CLAUDE.md §11 isi ko mana karta hai), aur drain `.brand` ko lay ja raha hai — jis din wo class gayi, **na error, na test, bas naam update hona band**. `plan.html` par ye **ho chuka hai**: naapa gaya, dono selectors **0** match (baqi aath par 1-1). **Ab ye khamoshi test se toot ti hai**, aur guard control se sabit hua: `slo.html` se `.name` hatane par test us ka naam le kar fail hota hai. Teesra test **ulta** likha hai — `plan` theek hote hi wo FAIL karega taake koi ye row band karna na bhoole. ~~Original row:~~ **`brand.js` sets the school name by querying two presentational classes, and on two of the ten pages it reaches nothing.** `js/brand.js`:22 and :27 run `querySelectorAll(".brand .name")` and `".brand .tag"` — exactly what CLAUDE.md §11 forbids ("never `querySelector` a presentational class — that is what welds styling to logic"). Two live gaps found while counting brand markup for UI-068, **both pre-existing and neither introduced here**: `print.html` does not load `brand.js` at all, so print's sidebar shows the hardcoded string while the other pages show the configured school name; and `plan.html` DOES load it but has no `.brand` and no `.name` — it is fully on `.sidenav__brand-name` — so the script runs and matches zero elements | UI-068, 2026-08-28 | It is a JS + settings bug, not a CSS drain, and fixing it inside a card/brand task is the drive-by §12.2 forbids. It is also not urgent: both pages show a correct-looking default | **Before UI-070** (dedupe the sidebar markup) and before item 9. Whoever takes it should re-point the script at `.sidenav__brand-name` / a `data-*` hook rather than at `.brand .name`, because the drain will delete `.brand` and this breaks silently — nothing fails, the name just stops updating |
| D55 | **The app paints two border greys and cards were the seam.** Legacy `--border` is `#E3E8F1` (`rgb(227,232,241)`); the new tree's `--color-border` is `--slate-200` = `#EAEDF3` (`rgb(234,237,243)`). UI-068 moved `.card` onto the component, which must read the Tier 2 role (CLAUDE.md §11), so **620 of its 1284 deltas were this one substitution** on 42 elements across six pages. That was a forced consequence, not a chosen value — the card decision Irfan took was radius/shadow/margin. Every remaining legacy family will hit the same seam the moment it moves up, and each one will look like "the drain changed a colour" **⚠ TWO MORE ELEMENTS MOVED ONTO `--color-border` ON 2026-08-29 (UI-072), AND IT WAS NOT A DECISION — IT WAS A TYPO BEING CORRECTED.** `bank.html`:104/:601 read `var(--line, #e2e8f0)`; `--line` is not declared on bank, so the fallback always painted and the page carried a THIRD grey belonging to neither side of this row. They now read `var(--color-border)`, matching the cards beside them. Recorded here because picking `--color-border` as the target IS this row's substance, and because it is a visible pixel change that shipped with no browser check. **✅ HALF OF THIS IS NOW SETTLED: Irfan looked at the rendered result on 2026-08-28 and accepted it.** So the new colour is NOT a defect and nothing needs reverting — what remains is only that the app still paints two border greys, and every family that moves up next will show the same seam | UI-068, 2026-08-28 | It is one substitution but its blast radius is the whole app, not one family — `--border` has consumers in all nine legacy files. Deciding it inside a card task would have been exactly the drive-by CLAUDE.md §12.2 forbids | Its own small session, or the first item-7 session, whichever comes first. The measurement is cheap (`css_selector_probe` on `--border` consumers); the decision is Irfan's and it is a one-word answer — and the answer is now likelier to be "make everything `--color-border`", since the eye test on 42 elements has already passed |
| D54 | **`--space-inset` is documented as "card / panel padding" (`theme.css`:176) and resolves to 24px, which no card uses.** All six bare cards measure **22px**, so `05-components/card.css`'s `.card` ships `padding: 22px` as a literal rather than point at a role that would move 42 elements by 2px under cover of a token. 22px is `--space-5`, so this is not an optical one-off that legitimately stays literal — **the Tier 2 role for it is simply missing**. Retuning `--space-inset` to 22px is not the fix either: `.card > .ch` and `.card > .cb` read it and do want 24px | UI-068, 2026-08-28 | A token decision, not a card decision. Either a new role (`--space-inset-card`?) or an admission that 24px is wrong for `.ch`/`.cb` too — both need Irfan and neither blocks the drain | Item 7 or the tokens pass. Whoever takes it should first count `--space-inset`'s consumers; today it is `card.css` ×2 and `pages/*.css`, a small list |
| D52 | **`index`'s three `.qtype` checkboxes compute `width: 100%` and a control-sized `min-height`, and always have.** They sit inside `display:flex` labels (`index.html`:118–120) and were painted by that page's bare `input, select` rule, which UI-066 kept bare precisely so they would not change. Chrome's `appearance:auto` checkbox ignores the border/background/font this also gave them, so the only live effect is the stretch. **Not measured against a design intent — nobody has looked at these in a browser** | UI-066, 2026-08-27, from the type probe: the first draft dropped them to UA defaults and the diff made the pre-existing values visible | Un-stretching them is a visible change to the teacher's main page and is **not** the sizing decision Irfan made on 2026-08-27, which was about the shared control set | One eye check first — if they look wrong, a two-line `.qtype { width: auto; min-height: 0 }`. Goes with item 7's `chip/pill/row` tail |
| D51 | **The three properties `forms.css` does not supply — `width`, `min-height`, `margin-top` — cannot leave `layer(legacy)` until stacked-field containers share a class.** Measured 2026-08-27 by putting them in `03-elements/forms.css` and reading the probe: `bank` `.opt-input-wrap input` 38px → 44px (16 elements) and `.float-bar` 34px → 44px, `blueprint`'s filter controls 38/36px → 44px, `print`'s modal controls `min-height` 0 → 44px and `width` auto → 100%, `slo-health`'s selects 157px → 903px with rows reflowed. **Six pages, four of them not in the field/filter family.** Every compact override in this app lives in `layer(legacy)`, so a base rule one layer up beats all of them whatever the specificity | UI-066, 2026-08-27 | The containers are four different names today — `.row`, `.field-row`, `.filter-row`, `.ps-row` — so the fix is a markup re-class across five pages, not a CSS move. That is its own task | With `slo`'s drain-map row 10 (`.row` → `.field-row`), which already proposes exactly this re-class. Until then the three stay per-page in `99-legacy/` |
| D50 | **AMENDED 2026-08-28 (UI-070 review): the same `.pg-btn.active` rule has a THIRD dead declaration, and it is the app's only authored "disabled but not forbidden" intent.** `99-legacy/library.css`:184 sets `cursor: default` on the active pagination button, and `library.html`:893 renders that button with BOTH `disabled` and `.active`. It was already inert at HEAD — `03-elements/forms.css`'s bare `button { cursor: pointer }` in layer(elements) beat it — and UI-070 changed what it loses to, from `pointer` to `not-allowed`. **That is inside Irfan's "every disabled button" decision, not a regression**, but somebody deliberately wrote "this one is not forbidden, it is just the page you are on" and the app has never rendered it. Decide it with the fill and radius below. Original row follows. **Two filled controls still paint the legacy navy on pages whose CTAs are now indigo — D27's defect, surviving UI-063.** `99-legacy/library.css`:188/191 `.pg-btn.active` is `background: var(--primary)` at radius 6px, and UI-063 edited `:192` in the same rule block while leaving it; `99-legacy/index.css`:234 `.lang-toggle button[data-active="1"]` is the same navy. Both are **segmented/pagination controls, not CTAs**, so their 6px/8px radius is correct and deliberately untouched — it is only the FILL that now disagrees with every action button on the same screen | UI-063, 2026-08-26, raised by review — I had reported the task as "one radius for every filled legacy button" and review showed the claim is only true for filled **light-surface action** buttons | Recolouring a selected-state control is a visible change to two pages and it is **not** one of the three decisions Irfan made on 2026-08-25 (radius, hover direction, disabled opacity). Doing it inside UI-063 would be exactly the drive-by this file exists to stop | One decision: does "selected" in a segmented control take `--color-action` like an action button, or is selected-state its own role? Belongs with the `chip/pill/row` family, item 7 of the finishing plan — both controls are that shape |
| D48 | **The on-dark button family has no Tier 2 role, so it keeps its own literals — and a filled button's shadow tint has no token either.** Cited by name from two comments UI-063 wrote (`99-legacy/bank.css`:282, `99-legacy/index.css`:100) **before this row existed**. Four members: `bank.css`:283-284 `.btn-fb-save` (`#5B8DEF`, hover `#3a6cdf` — it still DARKENS, so UI-063 narrowed the darken/lighten mix rather than ending it) and `.btn-fb-cancel`; `print.css`:89-93 `.ps-stepper button` (`#2b3f66`, hover `#38507f`, radius 6px) with `.ps-save` / `.ps-reset` beside it. Separately: `index.css`:99 `.gen-btn` and `btn.css`:288 `.btn-primary` both carry `box-shadow: … rgba(46,90,172,…)` — a **navy tint under a now-indigo fill**, already noted in `btn.css`'s header since 2026-08-16 but never given a row | UI-063, 2026-08-26. The on-dark carve-out was my own decision while taking the radius; the shadow tint and the `.ps-stepper` members were named by review | These need a token that does not exist. An on-dark surface role and an action-shadow tint are **palette decisions**, and inventing either inside a drain is how the tree got nine private greys in the first place | One task with two decisions for Irfan: (1) does the app get an on-dark action role, or does the dark bar keep literals permanently; (2) is the shadow tint derived from the fill. Not scheduled — it blocks nothing |
| D47 ⚠ | **(a) IS CLOSED — UI-070, 2026-08-28. (b) IS STILL OPEN AND IS STILL IRFAN'S.** The comment at `03-elements/forms.css` now states the cascade correctly: `:focus-visible` does NOT win by being later in the file — both rules are in `layer(elements)`, so source order never gets a say and `input:focus` (0,1,1) beats `:focus-visible` (0,1,0). A focused field's indicator is the teal border + 3px glow, and `outline: none` holds. The note now also says explicitly not to "fix" that as a side effect of reading it. **(b) — whether fields should ring on outline instead of glowing — is unchanged and untouched:** it would change focus on every field in the app, the instrument for measuring it (`outline-*` in `css_state_probe`) has been ready since UI-064, and Irfan confirmed by eye on 2026-08-25 that the current glow IS visible. Original row follows. **STILL OPEN, AND UI-063 WAS SUPPOSED TO CLOSE PART OF IT.** `ROADMAP.md`'s finishing plan scoped item 1 as "D44 + D46 + D47(b)". UI-063 closed D44 and D46 and **did not touch `forms.css` at all** — neither (a) the lying comment nor (b) the ring-vs-glow decision. Confirmed by review 2026-08-26: `forms.css` is not in the diff and the comment sits verbatim at :75-77. So item 1 landed **two thirds done**, and this was found by review rather than by any gate. Note also that `css_state_probe.mjs`:186-187 carries `outline-*` precisely so (b) could be measured — the instrument was built for this and went unused. **Original row follows.** **`03-elements/forms.css`:76's comment states the opposite of what the cascade does, and the file has been read as documentation four times since.** It says the `:focus-visible` rule at :106 beats the `input:focus` rule at :79 "being later in the file it wins". It does not. Both sit in `layer(elements)`, so **specificity decides**: `input:focus` is (0,1,1), `:focus-visible` is (0,1,0). During real keyboard focus BOTH match, so a focused input/select/textarea paints `outline: none` from :79 and gets its ring from that rule's teal border + 3px glow instead. Measured with `css_state_probe.mjs`: forcing `["focus","focus-visible"]` on `select#fSubject` gives `border-top-color rgb(14,165,164)`, `box-shadow rgb(220,245,244) 0 0 0 3px`, `outline-style none` | UI-065 (D45), 2026-08-25 — found by review running a direct CDP experiment while checking whether the new probe forced the right pseudo-class | **The rendering is not obviously wrong** — the glow is a real, visible focus indicator and Irfan confirmed by eye on 2026-08-25 that focus is visible on `bank`. So this is not a defect to rush a fix at; it is a comment that lies about which rule wins, in the file the tree treats as its forms reference. Changing the CSS to make `:focus-visible` actually win would change the focus treatment of every field in the app, which is a design decision nobody has made | two separable pieces: **(a) correct the comment** — small, safe, do it in any task that touches `forms.css`; **(b) decide whether fields should ring on outline or on glow**, and if outline, give `:focus-visible` the specificity to win. (b) belongs with the D44 button task, since that task settles the app's other focus surfaces |
| ~~D45~~ ✅ | **DONE 2026-08-25 — `scripts/css_state_probe.mjs`.** Forces `:hover` / `:focus` / `:focus-visible` / `:active` through CDP `CSS.forcePseudoState` and `:disabled` through the attribute, on all nine pages; **five states, ~30 s, 13,100 records, 0 errors**. It writes `css_type_probe.mjs`'s exact JSON shape, so **`css_type_diff.mjs` reads it unchanged** — no new diff tool. It carries `outline-*`, which `css_type_probe` does not have at all, and an outline is what a focus ring IS. **Proved by control, not by assertion:** mutating `.btn-cancel:hover`'s background on `bank` gives `css_type_probe` **0 deltas across all nine pages** and `css_state_probe` **1** — the exact class of change that slipped through UI-062. **Review FAILED its first version and the fix mattered:** it forced `focus-visible` alone, which does not match a `:focus` rule, so every `input:focus` in the repo — including the four UI-061 deleted — measured identical to rest, in the probe built to close that exact hole. Focus is now two passes. That fix also produced **D47**. See `PROBES.md` rules 10 and 11. ~~Old row:~~ ~~**Nothing in this repo can measure a hover, focus or active state, and two tasks in two days were bitten by it.**~~ `css_type_probe.mjs` snapshots at rest, so a declaration that only applies on `:hover` / `:focus` / `:disabled` contributes 0 deltas whether it is right or wrong. UI-061 removed four `input:focus` rules and had to justify them by layer-order argument because no instrument could confirm the paint; UI-062 re-pointed `.btn-cancel:hover` onto a different grey and **every gate passed without noticing**. `css_selector_probe.mjs --add` reaches class-driven state only — a pseudo-class cannot be added as a class | UI-062, 2026-08-25, found by review | Building a state probe is its own task: it needs CDP `Emulation.setEmulatedPseudoState` or forced-pseudo-class support, and a decision about which states are worth a regression gate. Doing it inside a drain would be the drive-by this file exists to prevent | one task: extend `css_type_probe.mjs` with a forced-pseudo-state pass over `:hover`/`:focus-visible`/`:disabled`, or add a `css_state_probe.mjs` beside it. **Do it before the D44 button task** — that task is entirely hover and disabled surfaces |
| ~~D46~~ ✅ | **DONE 2026-08-26 — UI-063.** Every `:disabled` rule in `static/css/` is now `opacity: .5` + `cursor: not-allowed`: `btn.css`:220/293/385, `blueprint.css`:56, `slo.css`:34, `index.css`:104, `library.css`:192, `print.css`:93/102. The .4/.55/.6 spread is gone and **`btn.css`'s header no longer describes a range that does not exist**. ⚠ **The `cursor` half of this is NOT verified by any gate** — see D49; only the `opacity` half produced measurable deltas (39 in the state probe, 3 at rest). ~~Old row:~~ ~~**`.btn-save:disabled` is `opacity: 0.6` two inches below `.btn--primary:disabled`'s `0.5`, in the same file.** UI-062 kept the measured legacy value rather than unify, correctly, and `btn.css`'s own header says so — but that note is a code comment, not a tracked row, so it exists nowhere a planning pass would see it. Across the nine legacy files disabled opacity runs .4 / .5 / .55 / .6~~ | UI-062, 2026-08-25, raised by review | Unifying disabled opacity is one decision over four values and touches every button family at once; smuggling it into a modal drain would change pages nobody was asked about | with the D44 button task — one commit that settles radius, hover direction and disabled opacity together |
| ~~D44~~ ✅ | **DONE 2026-08-26 — UI-063, and the app now has ONE action-button radius.** `.btn-primary` gave up its 10px literal for `--radius-control`; `.gen-btn` (13px), `.ghost-btn` (12px), `.btn-fb-save`/`.btn-fb-cancel` (8px) and the three surviving `--radius-btn` declarations (`blueprint`, `print`, `slo`) all resolve to 11px. Five dead `--radius-btn` declarations were deleted outright — **review confirmed all five were already unread at HEAD**, which is why `landing` and `slo-health` measure 0 deltas. **Measured, both probes:** 47 elements × 4 corners at rest, 235 × 4 across five states; the ONLY properties that moved app-wide are radius, `background-color` and `opacity`. ⚠ **The claim "every filled legacy button" is too strong and review corrected it** — it is every filled **light-surface action** button. The carve-outs are the on-dark family (D48) and two navy-filled segmented/pagination controls (D50). ~~Old row:~~ ~~**`.btn-primary` is 10px and `.btn-save` is now 11px, on the same page.** `btn.css`:277 keeps a deliberate 10px literal for `.btn-primary` with a recorded reason — *"that token is 11px and all three pages measure 10px here. The token would move every one of them."* UI-062 moved `.btn-save` / `.btn-cancel` onto `--radius-control` (11px) by Irfan's decision, so bank now paints a 10px primary button and an 11px save button in the same flow. Before UI-062 they agreed at 10px, because both read the same legacy `--radius-btn` | UI-062, 2026-08-25 — the modal drain. Measured, not inferred: `.btn-primary` `border-top-left-radius=10px` and `.btn-save` `=11px` on bank | **This is the residue of the 10px hold, not of the radius decision.** Closing it means moving `.btn-primary` (and `.btn-danger`, `.btn-fb-save` and the other filled legacy buttons) to `--radius-control` too — three-to-five files and a visible +1px on every filled button in the app. That is the "one button task" `modal.css` has been pointing at since 2026-08-16, and it is bigger than any single drain | one task: take every filled legacy button class to `--radius-control` in one commit, so the app has one button radius. Do it before any more per-family drains touch buttons~~ |
| D43 | **The topic-image strip's filter row lost its compact look, and it was already lost before anyone touched it.** `bank` and `print` both declare `.strip-filter input, .strip-filter select` at `font-size: 12px`, `padding: 4px 9px`, `border: 1px solid #D6DEEA`, `border-radius: 7px` — a deliberately small filter row next to a 68×52 thumbnail grid. **Not one of those declarations applies.** Measured with `css_selector_probe.mjs` on both pages: `font-size` **13.5px**, `padding-left` **11px**, `border-top-left-radius` **11px**, border `rgb(234,237,243)`. `03-elements/forms.css`:53 styles the same controls in `layer(elements)` and layer order beats specificity, so the legacy rule has been dead since the page joined `main.css`. Only `min-height: 30px` survived, because forms.css does not declare it | UI-061, 2026-08-24 — the `field/filter` agree drain. **This rule was NOT in the agree bucket and the first draft of this row said it was.** The audit classifies `.strip-filter select` as **disagree**: bank declares `color: var(--ink)` and print does not. It was drained anyway because the container rule beside it *was* agree and leaving the control rule behind made no sense — and the disagreement was resolved the conservative way, by carrying neither `color` nor anything else except the one live declaration. Both files' versions are equally dead, which is why the disagreement turned out not to matter; that is a measured result, not the reason it was picked up | **Restoring it is a design decision, not a drain.** The question is whether a filter row inside a dense picker should be smaller than a form control — if yes it is a real variant (`.field--compact` or similar) that several pages could use; if no, the 12px was an intention the app has silently not honoured for weeks and the rule should just go. Guessing either way inside a drain task would change two live pages under cover of a cleanup | one task, after Irfan decides: either author the compact control variant in `05-components/field.css` and apply it on `bank` + `print`, or close this row and delete nothing further |
| D42 | **The sidebar is not the same nav on every page, and no page is wrong on its own.** Counted 2026-08-23: `index`, `slo`, `slo-health`, `taqseem` and the new `plan` carry the full 8-link nav; **`bank`, `library` and `blueprint` carry 5** — they link neither `slo.html` nor `taqseem.html` nor `plan.html`, so from the Question Bank there is no way to reach Learning Outcomes without typing the URL. `print.html` has a `.app-nav` with no `.sidenav__link` at all. A teacher's route through the app therefore depends on which page they happen to be standing on | R7 Marhala 4, 2026-08-23 — adding "Hafta-war Plan" made it obvious: the link could only be added to the 5 pages that already had a planning group to add it to | Adding three links to three pages is one edit, but the QUESTION is not which links to add — it is whether the nav should be one list in one place. Right now the same 8 anchors are copy-pasted into 5 files, which is the same duplication this epic exists to remove, and doing it a 6th, 7th and 8th time makes that worse while looking like a fix. `landing.html` is a separate shape again (`.qa-card` grid) | one task: decide whether the nav is a shared fragment (needs a JS include or a server-side template — the app has neither, ADR-001 forbids a build step) or stays duplicated with a test that asserts all pages carry the same link set. The test is the cheap half and could ship alone |
| ~~D40~~ ✅ | **HO GAYA 2026-08-21.** Field ab shared `QuestionType` use karta hai (do alag list rakhna hi bug tha), aur bank.html mein essay ka dropdown option, list filter aur apna badge shamil. Essay short-answer ka form section share karta hai — apna section teen naye id maangta jo frozen inventory badal kar `--write` par majboor karta, aur wo abhi multawi hai. AI-generated essays ka round bhi ho gaya (11 essay bank mein). Model answer form abhi bhi nahi poochta, magar short-answer bhi nahi poochta — wo alag sawal hai. 12 tests, purane Literal par 7 fail hoti hain. Tafseel PROGRESS.md 2026-08-21. ~~Purani row:~~ ~~**`ManualQuestionRequest` ka Literal `essay` ko nahi jaanta** (`app/schemas/requests.py`). Teacher haath se essay sawal nahi likh sakta, jabke `_PAPER_TYPE_FILTERS`, `_RATIO_SUBJECTIVE_GROUP`, `bloom_service` (`"essay": 3`) aur `ai_service` sab use support karte hain, aur `print.html` ka `answerSpace()` essay ke liye alag diagram box banata hai. 2026-08-20 ko DB mein essay ke **sifar** sawal the — dono baatein shayad ek hi wajah ki hain | Sections mode, 2026-08-20 — sections "Essay" ka section bana sakte hain magar bank mein wo sawal aayega kahan se | Literal barhana chhota hai, magar sawal ye hai ke manual entry mein essay ka UI (options nahi, model answer haan) pehle se theek hai ya nahi — wo naapna is task ka hissa nahi tha | ek chhota task: Literal + bank.html ka manual form, aur AI-generated essays ka ek round |
| D2 | 31 stale git branches (`feature/*`, `hissa-*`, `fix/*`) | audit 2026-07-28 | git hygiene, unrelated to UI | a `chore/branch-cleanup` session |
| ~~D3~~ ✅ | **DONE 2026-08-31, with O1.** §4's DevOps row and §6's Production row are struck and replaced: one environment (local), one `backup` remote that is not a host. §6's rule *"never assume local data exists in prod"* was inverted at the same time — there is only one DB and it holds the teacher's real data, so the rule now says take a backup before seeding. Closed as part of the O1 decision rather than as a doc fix, because the row was never really about wording: it described a deploy flow that does not exist. ~~Original row:~~ ~~`docs/SDLC-WORKFLOW.md` §4/§6 still describe Railway auto-deploy~~ | audit 2026-07-28 | Railway/Northflank is dead; Irfan pushes via GitHub Desktop, no auto-deploy | doc fix, after this epic |
| ~~D4~~ ✅ | ~~`IMPLEMENTATION_HANDOFF.md`, `REDESIGN_SPEC_blueprint.md`, `ROLLOUT_all_pages.md` all specify the **Classic navy/gold** palette, superseded by Modern~~ | audit 2026-07-28 | ~~three stale specs will mislead a future session~~ | **DONE 2026-08-22** — teenon `docs/design/archive/` mein `git mv` ho gayin (repo root se hat gayin), aur har ek ke oopar ⛔ tanbeeh lagi hai jo batati hai kya supersede hua aur aaj asal jagah kaun si hai. UI-064 ka intezar nahi kiya: `ROLLOUT_all_pages.md` §1 aaj bhi `static/theme.css` "install/replace" karne ko kehti thi jabke wo file UI-064 part 1 mein delete ho chuki — yani ye sirf gumraah-kun nahi, **ulta chalane wali** thi. Hawale naape gaye: teenon ka koi bahar se link nahi tha, sirf apne andar ke aur yehi row |
| ~~D5~~ ✅ | **BAND 2026-09-02 (UI-075, item 9) — dono files `docs/design/` mein `git mv` ho gayin**, aur `tests/test_css_architecture.py` ab dono ki jagah assert karta hai taake koi wapas `static/` mein na girey (wahi haadsa jo `ui-check.html` ke saath hua tha aur saat test tore the). ⚠ **Is row ka bayan aadha ghalat tha:** "are served as live app URLs" sirf `mockup-modern.html` par sach tha — `papermaker-mockup.html` **repo ki jar par pari thi**, aur wahan `/static` mount pohanchta hi nahi, is liye wo kabhi serve nahi hoti thi (naapa: dono par ab `404`, aur jar wali par pehle bhi). Ye is task ke dauran do baar ghalat naapi gayi: pehli koshish mein maine sirf `static/*mockup*` dekh kar likh diya ke doosri file mojood hi nahi — wo mojood thi, sirf kahin aur. **`ls <ek jagah>` "mojood nahi" ka saboot nahi hai.** Ye row `UI-064` ka intezar bhi kar rahi thi; wo task apne aap band ho gaya tha aur ye saath nahi gayi | audit 2026-07-28 | they are design references, not pages | ~~move to `docs/design/` in UI-064~~ — **ho gaya, item 9 mein** |
| ~~D7~~ ✅ | `.btn.gold` renders **teal** under the Modern palette — the class name lies | audit 2026-07-28 | rename is scheduled, not a bug | **RESOLVED 2026-08-11 by UI-041b**, not by UI-041 as this row said. `05-components/btn.css` ships `.btn--accent`, named for what it is. It does NOT read `--color-accent`: white on teal-500 is **3.03:1** and the button is 15px/700, under WCAG large-text, so it needs 4.5:1 — the legacy `.btn.gold` fails that on `taqseem` today and the port would have carried it. Irfan chose to darken the fill rather than the text so both filled variants keep white text: `--teal-600` at **6.07:1**, beside `.btn--primary`s 6.29:1. Prepared, not live — `.btn--` is in zero markup; activates with `UI-047a` |
| D8 | `landing.html` exists but `/` still serves `index.html`; landing is unreachable in normal use | audit 2026-07-28 | routing/product decision, not styling | ask Irfan after the epic |
| D9 | `print.html` and `landing.html` never loaded `theme.css` at all | audit 2026-07-28 | resolved naturally by the migration | UI-013 / UI-018 |
| D10 | Repo root holds 11 `paper_maker*.db` backups + `app.zip` / `.claude.zip` / `config.zip` | audit 2026-07-28 | working-tree hygiene; backups should not live in the repo root | a `chore/repo-hygiene` session (ROADMAP H2 already tracks this) |
| D11 | `compare_js_classes` only catches **JS-side** renames. All 32 load-bearing class names appear in a `<script>` body, so the `class="..."` half of the search is doing no work; a class renamed **in markup only**, with stale JS left behind, passes the check. Measured, not theoretical. | UI-002 review 2026-07-28 | closing it needs per-class, per-half baseline state — a design change to `BASELINE.json`, not a tweak. The docstring states the limit honestly, and the frozen id/onclick/data-* inventory (which *is* exact) already covers the attribute half of the threat | a follow-up ratchet task, or fold into UI-050 when inline styles are burned down |
| D12 | CSS built in JS is invisible to every metric — `el.style.cssText = ...`, `sheet.insertRule(...)`, or a template literal with no literal `<style>` tag. No `<style>` element, no `style=` attribute, no `.css` file, so `shared_css_lines` does not close it either | UI-002 review 2026-07-28 | no occurrences today; detecting it reliably means parsing JS, which is well beyond a line-counting ratchet | revisit only if a page starts generating CSS at runtime |
| D13 | `static/brand/logo.svg` hardcodes `fill="#2E5AAC"` — the same Classic blue deleted from the brand config in UI-003 | UI-003 review 2026-07-28 | an SVG cannot read a CSS token or the API, so this is an asset change (recolour or `fill="currentColor"`), not a CSS one. No ratchet metric counts it: the ratchet reads `.html` and `.css` only | UI-064, alongside the other asset/palette reconciliation |
| D15 | `blueprint.html`'s `@media (max-width: 760px)` block styles `.app-sidebar`, `.app-nav`, `.sidebar-foot` and `.bp-main` — **none of which exist** in `blueprint.html`, `app.css` or `theme.css` (grep is empty for all four). Dead responsive CSS; the page's actual mobile behaviour comes from the chrome in `theme.css`. Now at `99-legacy/blueprint.css:269-276` | UI-015 2026-07-29 | pre-existing at HEAD, not caused by the extraction. Deleting it mid-Sprint-1 would be a real change inside a task whose contract is *zero* visual change, and it needs a browser check at <760px to confirm nothing actually depends on it | UI-051/052 inline burn-down, or the `99-legacy/` kill in Sprint 6 |
| D16 | `docs/ui/PLAN.md` §Sprint-1 describes `print.html` as having **5 `@media print`** blocks. It has **2**, plus 1 `@page` and 1 `@media (max-width: 760px)`. Verified twice with CSS comments stripped (implementer + review agent) — a raw grep over-counts because the block's prose comments mention `@page` twice and `@media print` once. STATUS.md's UI-017 handoff repeated the same raw-grep error ("3 / 3") and was corrected in UI-018 | UI-018 2026-07-29 | not silently reconciled (§12 rule 11). PLAN.md's Sprint 1 table is now historical — the sprint is done — so the wrong figure misleads nobody mid-flight, and editing another planning doc inside an extraction task is exactly the drive-by §12 rule 2 forbids | UI-064, alongside the other stale-doc fixes (D3/D4/D5) |
| D17 | `docs/ui/PLAN.md` §2 writes its own `01-settings/tokens.css` example as a **single line** (`:root { --indigo-600:#4f46e5; --teal-500:#0ea5a4; ... }`). `test_tokens_file_holds_only_token_hex` (added UI-020a) is line-based — it exempts any line carrying a `--name:` declaration in full — so a session that copies PLAN.md's formatting satisfies the guard **vacuously**, and could park a component rule on the same line undetected. UI-020 therefore authors the real file one declaration per line, which is what makes the check bite | UI-020a review 2026-07-30 | the guard's limitation is now stated honestly in both docstrings rather than oversold, and the file UI-020 actually ships is formatted so the check works. Editing PLAN.md's example is a planning-doc change inside a task that owns neither — the drive-by §12 rule 2 forbids | UI-064, with the other stale-doc fixes; or fold into UI-021 if `02-generic`/`03-elements` need the same formatting rule stated |
| D18 | `test_token_hex_only_exempts_the_one_sanctioned_file` (UI-020a) cannot catch a broadening of `token_hex_count()` that is **derived from `TOKENS_PATH`** — e.g. `TOKENS_PATH.parent.glob("*.css")`. The test monkeypatches `TOKENS_PATH` to a decoy in a tmp dir, so any parent-relative implementation follows the patch into a directory that holds only the decoy and the arithmetic still checks out. Measured by the UI-020a review agent across four mutations: returning `stylesheet_hex_count()`, hardcoding the real path, and a blanket `static/css/**` amnesty are all **caught**; the `.parent.glob` form is **not** | UI-020a review 2026-07-30 | structural to the monkeypatch approach, and it pre-dates the fix — neither the broken nor the corrected assertion ever detected it, so nothing regressed. Practical blast radius is small: the only sibling `01-settings/theme.css` is Tier 2, which must carry no raw hex anyway, so a `.parent` broadening would exempt a file the ratchet already requires to be hex-free. Closing it needs a fixture that plants a **second** decoy alongside the first | a follow-up ratchet task, or fold into UI-021 when `01-settings/` gains its second real file |
| D19 | `main.css` imports **all nine** `99-legacy/<page>.css` files into `layer(legacy)`, so once a page's only stylesheet is `main.css` it will load all nine. They were nine separate per-page `<style>` blocks and they define the same generic class names with different values — `.card` (6 files), the button classes across 5 files (`.btn-ghost` in 5, `.btn-primary` in 3), `.modal` (4), `.page-head` (4), plus `:root` in all 9 — so every page would get every page's version and the **last import would win for all of them**. Visible breakage, not a purity issue | UI-020 2026-07-30 | **not a deviation — `docs/ui/PLAN.md` §2 specifies exactly this** ("`99-legacy/*` is imported **first** so everything in the new tree wins without a single `!important`"), and it has to be an `@import ... layer(legacy)` rather than a `<link>`, because a plain `<link>` is *unlayered* and unlayered styles outrank every `@layer` — linking would invert the cascade `main.css` exists to establish. Harmless today: nothing links `main.css` until Sprint 3, verified by serving the app (0 pages reference it). Fixing it inside UI-020 would mean redesigning the documented load mechanism in a task scoped to authoring tokens, which §12.2 forbids | **UI-031 / UI-032**, which are the tasks that put the single `<link>` on a page and therefore cannot avoid it. Two candidate shapes: scope each legacy file under a `body[data-page="x"]` wrapper, or drop the shared legacy imports from `main.css` and keep the per-page legacy `<link>` inside a `<style>@import url(...) layer(legacy)</style>`-style shim so it stays layered. Decide there, with a browser open |
| D20 | **The `99-legacy/` layer depends on `static/theme.css` for 21 custom properties that `main.css` does not supply.** Measured: the nine legacy files reference **49** distinct custom properties and declare **30** themselves, so **24** must come from outside; `main.css`'s `layer(settings)` satisfies exactly **3** (`--font-display`, `--font-body`, `--font-data`). The other 21 — `--accent`, `--accent-soft`, `--app-bg`, `--brand`, `--brand-2`, `--chip-bg`, `--fg`, `--green`, `--green-bg`, `--panel`, `--panel-2`, `--radius`, `--radius-sm`, `--red`, `--red-bg`, `--sidebar-active`, `--sidebar-bg`, `--sidebar-fg`, `--yellow`, `--yellow-bg` (all from `static/theme.css`) and `--text` (**from nowhere — already inert, that is D14**) — would resolve to nothing. Consumers are `blueprint.css`, `taqseem.css`, `print.css` and `library.css`, but **the live blast radius is 2 pages, not 4** — measured at the UI-020 review: `blueprint.css` loses 17 tokens and `taqseem.css` 20, **none of them fallback-protected**, whereas `print.css`'s only orphan (`--accent`) carries a `#0e4d3c` fallback on all 4 uses *and* `print.html` never linked `theme.css` anyway (D9), so Sprint 3 changes nothing for it; `library.css`'s `--text` is already inert today (D14). So a Sprint 3 session that makes `main.css` a page's only stylesheet and drops the `theme.css` link ships pages whose colours, radii and sidebar all fall back to initial/inherited: **big visible breakage, and no ratchet metric or test would catch it** — the ratchet counts lines and hex, not whether a `var()` resolves | UI-020 review 2026-07-30 | **this is the half D19 does not describe.** D19 is about class-name collisions between the nine legacy files; this is about token *dependencies* pointing at a file the new tree does not replace. A session that reads only D19 will scope the class collisions, drop `theme.css`, and hit this. Fixing it inside UI-020 would mean authoring Tier 2 aliases for 20 legacy token names — component work in a task scoped to the palette, which §12.2 forbids — and the aliases belong wherever the pages are actually migrated | **UI-031 / UI-032**, with D19; they are the same decision. Either keep the `static/theme.css` link until each page's legacy file is drained (Sprint 6), or add a temporary compatibility block mapping the 20 legacy names onto Tier 2 roles and delete it in UI-064. `--text` needs no alias — fix or delete it per D14 |
| D21 | **A "reset" in this tree is not neutral — it overrides the legacy layer.** `main.css` declares `@layer legacy, settings, generic, elements, ...`, and layer order beats selector specificity for normal declarations, so a *one-element* selector in `02-generic`/`03-elements` outranks every *class* rule in `layer(legacy)`. The design target opens with `* { box-sizing:border-box; margin:0; padding:0 }` (`mockup-modern.html:15`) and is right to, having no legacy layer beneath it; ported verbatim the `padding:0` half beats `99-legacy/index.css:267`'s `[dir="rtl"] .question-card ul { padding-right:20px }` and flattens list indentation in Urdu mode. **No ratchet metric or test sees this** — the ratchet counts lines and hex, not whether a list still reads | UI-021 2026-07-30 | UI-021 worked around it rather than deferring the whole problem: the universal padding kill is **not** ported, the margin reset is targeted at block text elements only, and `img` gets `max-width` without the usual `display:block`. Each omission is stated in the file that omits it. What is deferred is the *general* question — every future `02-generic`/`03-elements` rule has to be checked against the legacy layer, and there is no automated guard for it. This is a third failure mode alongside D19 (class-name collisions between the nine legacy files) and D20 (legacy tokens pointing at `static/theme.css`); a session that reads only those two will not expect it. **One consequence is parked here rather than in a row of its own:** `html, body { height: 100% }` is in the design target (`mockup:16`) and in **7 of the 9** legacy files, but it is *not* in `reset.css` — height is layout, which is `04-objects/shell.css`, not one of the three concerns PLAN.md §2 gives that file. The flex/grid app shell needs it, so **UI-030 must land it** or the shell will not fill the viewport — and when it does, the rule newly reaches the **two** files that lack it: `landing.css:18` (has the `html, body` rule, margin only) and `print.css` (no `html, body` rule at all; `print.css:24` uses `display:flex; min-height:100vh` instead). **print.html is the one to check in a browser** — it is the Ctrl+P page under `@page { margin: 0 }` that `STATUS.md` calls the epic's highest visual risk. Counted per file: this row and `reset.css` both said "8 of 9, landing is the exception" in their first draft, which hid the print.html half | **UI-031 / UI-032**, the same browser-open session that resolves D19/D20 — and it is the same decision for the same reason. Worth a targeted check at <760px and in Urdu mode. ~~The `height: 100%` half belongs to **UI-030**~~ — **CLOSED at UI-030 as unnecessary, not deferred again.** The prediction was wrong: the design target carries *both* `html,body{height:100%}` (`mockup:16`) and `.app{height:100vh}` (`mockup:58`), and the second is what fills the viewport — the first is redundant to it. `04-objects/shell.css`'s `.o-shell` uses the same `100vh`, so the shell fills the viewport without the rule, and the print.html risk this row was written to flag is never taken. A later task that genuinely needs percentage heights inside the shell can add it then, with `print.html` open |
| D22 | **The design target's button reset is deliberately NOT in `03-elements/forms.css`.** `mockup-modern.html:19` has `button { font-family:inherit; cursor:pointer; border:none; background:none; color:inherit }`. The last three declarations, landed in `layer(elements)`, would flatten every filled button in the app the moment a page's only stylesheet is `main.css`: buttons are drawn by class in `layer(legacy)` — `.btn-ghost` in five of the nine files, `.btn-primary` in three — and the element rule outranks all of them (D21). `static/theme.css`'s `.langsw button` (:77) and `.segbtns button` (:163) are **not** part of that argument and were wrongly cited as such in this row's first draft: that file is a plain unlayered `<link>`, so it beats every `@layer` while it is loaded and is gone entirely once it is not. Which way a rule resolves depends on whether it arrived layered, which is D19/D20's subject. UI-021 ships only `font-family: inherit; cursor: pointer`. **The `font` shorthand is a fourth trap in the same rule, and UI-021's first review caught it there**: `font: inherit` reads as a synonym for `font-family: inherit` and is not — it also sets font-size, font-weight, font-style, font-variant, font-stretch and line-height, so from `layer(elements)` it drags every legacy button to the body's 15px/400 and strips the weights they set by class (`99-legacy/slo.css:52` `.btn` 14px/600, `:60` `.btn-ghost` 13.5px/600, `bank.css:348` 13px/700, `library.css:118` 13px). It shipped as the shorthand, was caught in review, and is now `font-family` | UI-021 2026-07-30 | landing the appearance reset needs a `.button` component in `layer(components)` to win it back **in the same commit**, which is UI-041's declared scope. Doing it in UI-021 would mean authoring a component inside a task scoped to generic + elements (§12.2), and would leave the tree in a state where Sprint 3 breaks buttons | ~~UI-041~~ — **UI-041 LANDED 2026-08-06 AND DID NOT TAKE THIS, BY MEASUREMENT.** This row's prescription does not work and the reason is worth keeping: it assumed a component would "win it back in the same commit", but a component only wins back elements that carry ITS class. The live pages' markup still says `.btn-primary`/`.btn-ghost`, not `.btn--primary`, so the element reset would flatten them from `layer(elements)` and the component would never see them — 63 elements carrying a legacy `.btn*` class on `library` alone (69 counting `.pg-btn`, 71 `<button>` elements in all — the row actions render per record, so all three move with the database), plus 4 buttons and one `<a class="btn-ghost">` on `slo` and 1 on `slo-health`, all on pages a teacher uses today. **The reset can only land in the commit that re-classes the markup**, i.e. per page as each legacy file drains (Sprint 6), or in one sweep once no page carries a legacy button class. UI-041 did not open `03-elements/forms.css` at all (`git diff` empty). **Still OPEN. Do not schedule it against a component task again** |
| D23 | **`--font-mono` names a family nothing provides.** `01-settings/tokens.css`'s `--font-mono: "IBM Plex Mono", ui-monospace, monospace` — and `static/theme.css`'s `--font-data`, which predates this epic — name **"IBM Plex Mono"**, but no `@font-face` anywhere declares it and no `IBMPlexMono-*.woff2` is in `static/fonts/` (verified: 9 woff2 files, 4 IBM Plex Sans + 3 Source Serif 4 + 2 Noto Nastaliq Urdu). So `--font-data` always falls through to `ui-monospace` — which looks fine, which is why it has gone unnoticed. Consumers today: `static/theme.css` `.tag` / `.bloom .pct`, and now `03-elements/typography.css`'s `code, kbd, samp, pre` | UI-021 2026-07-30 | the fix is a **font asset decision**, not CSS: either add the two woff2 files (~80KB, and they must be licence-checked and committed) or drop the family from the stack and let `ui-monospace` be the declared intent. Both are Irfan's call, and neither belongs in a task scoped to authoring generic + elements. Not fixed by accident either — `02-generic/fonts.css` absorbs `static/app.css` **verbatim in coverage**, and app.css never had a Plex Mono face | ask Irfan, then either a small asset task or **UI-064** with the other palette/asset reconciliation (D13). Stated in both token files so no session assumes the family loads |
| D24 | **`nav.mypapers` is referenced by `index.html`'s nav markup but has no entry in its translation table.** The table carries `nav.generate`, `nav.syllabus`, `nav.adaptive`, `nav.results`, `nav.analytics` and `nav.settings` in both English and Urdu — `nav.mypapers` is in neither, so the "My Papers" item does not translate when the page switches to Urdu; it keeps whatever the markup hardcodes. Two related oddities in the same table, recorded but not touched: `nav.syllabus` and `nav.results` have translations but are **not** nav items — `results` is a screen reached another way (`showScreen('results')`, 4 call sites), and `syllabus` has neither a nav entry nor a `showScreen` call, though its `data-screen="syllabus"` section is still in the markup, so it is an orphaned screen rather than a deleted one, and the six page links — Question Bank, Image Library, Blueprint, Learning Outcomes, Exam Taqseem, SLO Health — carry no `data-i18n` at all, so they are untranslated on every page today | UI-030 2026-07-31 | found while measuring the real nav to author `config/nav.json`; the config records the key so nothing is lost. Fixing it means editing `index.html`'s markup or its i18n table, and UI-030 authors **no markup and no JS** — a page edit inside a task scoped to a stylesheet plus a config file is exactly what §12.2 forbids. It is also not a CSS defect at all, so no ratchet metric moves either way | **UI-032**, which migrates `index.html` and is already in that file's markup; or a standalone i18n fix at any time — nothing blocks on it. Decide with Irfan whether the six page links should become translatable too, since that is a product question, not a cleanup |
| D25 | **Four nav destinations have no address, and that is why the nav drifted into three versions.** `mypapers`, `adaptive`, `analytics` and `settings` are SPA screens inside `index.html`, shown by `showScreen(...)`; they have no URL, so a page that is not `index.html` has no way to link to them. Measured at UI-030: `bank`/`blueprint`/`library`/`print` carry a 5-item nav, `index` 10, `slo`/`slo-health`/`taqseem` 7, and `landing` none. **Only half of that is explained by the missing addresses, and the first draft of this row wrongly claimed all of it was** (caught in review): the three 7-item pages carry exactly the 7 `url` items, so for them the four unlinkable screens are the whole difference — but the four 5-item pages additionally drop `/slo.html` and `/taqseem.html`, which have perfectly good URLs and are linked from the 7-item pages. So the 5-vs-7 split **is** arbitrary drift, and the 7-vs-10 split is *mostly* structural — not entirely: it is +4 unlinkable screens **and −1 linkable item**, because `index.html` carries no Generator / `/` entry at all while every other page does. That is not a self-link convention either — the 7-item pages do self-link (`slo.html`'s nav includes `/slo.html`, and so on for `taqseem` and `slo-health`). **UI-032 has to decide whether rendering this config on `index.html` adds a Generator item that is not there today.** Both of the tidier claims this row made first — "exactly the items that cannot be linked", then "only the 7-vs-10 split is structural" — were caught in review; the counts were never wrong, the conclusions drawn from them twice were. `config/nav.json` therefore marks each item `url` or `screen` and **declines to invent a convention** for the screen half (`/#mypapers`, `/?screen=mypapers`, or making them real pages are all plausible and all have JS attached) | UI-030 2026-07-31 | UI-030's scope is `04-objects/shell.css` + the nav config as **data**; the option Irfan chose explicitly excludes a JS nav renderer, and picking an address scheme is a routing decision that changes `index.html`'s script, not a layout one. Deciding it here would also pre-empt the task that has to implement it | **UI-032** (it owns `index.html` and its `showScreen` handlers), informed by **UI-031** having already rendered the config on the five simple pages. Until it is decided, a migrated non-index page can only render the 7 `url` items — which is what those pages show today, so nothing regresses |
| D26 | **`.brand small` lands at 3.04:1 on the dark sidebar — under WCAG AA, though it is an IMPROVEMENT on what was there.** `03-elements/typography.css`'s `small { font-size: var(--text-micro); color: var(--color-text-muted) }` is an ELEMENT rule in `layer(elements)`, so it outranks `99-legacy/slo.css:26`'s `.brand small { font-size: 10.5px; color: #9DB0D0 }` — a *class* rule in `layer(legacy)`. That part is D21 arriving for real. Measured in headless Edge 150 on `slo.html`, computed styles plus the **painted ancestor background**, both sides: before `rgb(157,176,208)` at `opacity .7` over **`#ffffff`** — because `static/theme.css`:62 `.brand { background: var(--panel) }` painted a white box inside the navy sidebar — composites to `rgb(186,200,222)` on white = **1.70:1**; after `rgb(100,116,139)` at full opacity on `#16294A` = **3.04:1**. So contrast nearly doubled; it is still under AA's 4.5:1 for text that size, which is why this row exists. The subtitle also loses `text-transform: uppercase`, `letter-spacing: .6px` and `opacity: .7`, all from `theme.css`:67. `--color-text-muted` is slate-500 and is correct for muted text **on the light canvas it was designed against**; the sidebar is the one dark surface in the app and the token has no dark-surface counterpart, so every page UI-031/UI-032 migrates hits it | UI-031 2026-07-31, in the browser | UI-031's scope is one page's `<link>` block plus the entry-file mechanism. The three ways to fix it are all somebody else's task: a `--color-text-on-dark` role is `01-settings` (Sprint 2, closed), narrowing `small` so it sets size but not colour edits a reviewed UI-021 file and changes every page, and the right fix — the sidebar being a **component that owns its own text colours** — is `05-components/nav.css` in Sprint 4. Patching `99-legacy/slo.css` is not an option: that file is append-never | **UI-040/041** (nav + component colours), or a one-line `--color-text-on-dark` token if Irfan wants AA before the sidebar component lands. **This row's first draft FAILED review and deserved to**: it claimed "6.59:1 → 3.04:1, a legibility regression", having measured both colours in the browser and then computed the *before* ratio against an assumed `#16294A` backdrop instead of the white box `theme.css` actually painted. The colours were measured; the backdrop was assumed; the conclusion came out inverted. Same defect class as UI-020's "linked by 8 pages" and UI-030's "12 items" — **check what is painted, not what you expect to be painted** |
| D27 | **`<a class="btn-ghost">` loses its blue; `<button class="btn-ghost">` keeps it — same class, two appearances on one screen.** `typography.css`'s `a { color: inherit }` (element, `layer(elements)`) outranks `99-legacy/slo.css:60`'s `.btn-ghost { color: var(--primary) }` (class, `layer(legacy)`), but nothing touches `<button>`. Measured before vs after on `slo.html`: the "Template download" `<a>` goes `rgb(46,90,172)` → `rgb(15,23,42)`, while `#exportBtn` and `#filterBtn` — same class, same card — stay `rgb(46,90,172)`. So the page now shows three `.btn-ghost` controls in two different colours. `a { color: inherit }` itself is not the defect and must not be reverted: **8 of the 9 legacy files declare exactly that rule**, it is what stops link-buttons rendering as browser-blue underlined links, and `typography.css` says so in its own comment. The defect is that buttons are drawn by class in a layer that a bare element selector outranks — which is **D22's argument, arriving one rule earlier than D22 predicted**: D22 parked `border`/`background`/`color: inherit` from the *button* reset for exactly this reason, and `a`'s colour reaches the same controls through the anchor half nobody parked | UI-031 2026-07-31, in the browser | the fix is the `.button` component in `layer(components)`, which outranks both and paints `<a>` and `<button>` identically — that is **UI-041's declared scope**, and D22 already parks the matching half of it there. Doing it here means authoring a component inside a task scoped to one page's `<link>` block (§12.2), and doing it in `99-legacy/` is barred outright | **UI-041 landed the fix 2026-08-06 and the defect is still live — those are both true.** `05-components/btn.css`'s `.btn--ghost` sets `color` explicitly, so an `<a>` and a `<button>` carrying it paint identically; the component is written and imported. It reaches no element, because no markup carries `.btn--ghost` yet. **The defect clears per page, in the commit that re-classes that page's markup, not when the component landed.** This row's prediction — "every page UI-031/UI-032 migrates that has an `<a class="btn-ghost">`" — was confirmed by measurement on 2026-08-06: `library` has the same split (`rgb(15,23,42)` on `a#metaTemplateBtn` vs `rgb(22,33,58)` on its sibling `<button>`), so the live count is **2 pages, not 1**. **Still OPEN** |
| D28 | **A migrated page loses its intra-card spacing: `reset.css`'s margin reset zeroes three legacy class margins, while `typography.css` makes the headings above them bigger.** `02-generic/reset.css`:79–92 sets `margin: 0` on `body, h1..h6, p, figure, blockquote, dl, dd` in `layer(generic)` (the declaration itself is at :91); `layer(legacy)` is below it, so the *class* rules that carried the spacing lose. Measured on `slo.html` in headless Edge 150, element-aligned, before → after: `.card .hint` ×3 `0 0 16px` → **`0`** (16px of breathing room under every card heading, gone), `.card h2` ×3 `0 0 4px` → `0`, `.page-head p` ×1 `5px 0 0` → `0`. In the same pass `03-elements/typography.css`:66–77 takes `.page-head h1` **22px → 24px** (tracking `normal` → `-0.2px`) and `.card h2` ×3 **15px → 18px**. Net: headings ~20% bigger with zero gap beneath them. Card 1 `158px` → `143.05px`, card 2 `224px` → `214.05px`, `.page-head` `52px` → `45.95px`. Also in this bucket, harmless: `forms.css`:**101–102**'s `font-family: inherit` takes the two `<button class="btn-ghost">` off **Arial** onto IBM Plex Sans (the `<a>` was already Plex — anchors inherit, buttons do not; note `:58` carries the same declaration but its rule is `input`/`select`/`textarea` and does **not** match a button — this row's first draft cited `:58`/`:102` and both were wrong), `:63`'s `--radius-control` takes the five `select`/`input` 9px → 11px with `:62` shifting their border to `--color-border`, and `typography.css`:97-103's `code, kbd, samp, pre` takes five inline `code` 12.5px → 13.5px onto `"IBM Plex Mono", ui-monospace, monospace` — **D23's first live appearance**, since no `@font-face` declares that family and no woff2 in the repo provides it. **This is D21 firing on a real page for the first time** — the row that predicted a reset in this tree would not be neutral, written before any page loaded `main.css` | UI-031 2026-07-31, in the browser | the reset is correct and must not be narrowed to win the spacing back: `layer(generic)` zeroing margins is the whole premise of the new tree, and the spacing is supposed to come from the components that replace those legacy classes. Restoring it inside UI-031 means either authoring `.card`/`.pagehead` components in a task scoped to one page's `<link>` block (§12.2), or editing `99-legacy/slo.css`, which is append-never. UI-021 already narrowed this reset once (the universal `padding: 0` was not ported, D21) and narrowing it again to protect legacy class margins would make the reset useless on the pages it is actually for | **UI-040** (`card` + `page-head` → `.card`/`pagehead`, which is where the padding and heading spacing land) with **UI-041** for the buttons. Until then every page UI-031/UI-032 migrates ships with tighter cards. **Flagged to Irfan at handover, not shipped silently** — it is visible, it affects all nine pages, and no ratchet metric sees it |
| ~~D29~~ ✅ | **BAND — jaise is row ne khud peshgoi ki thi: "the collision dies on its own in UI-064 when `static/theme.css` is deleted."** Wo file UI-064 mein delete hui; `ls static/theme.css` par kuch nahi. Ab repo mein `theme.css` naam ki **ek hi** file hai (`static/css/01-settings/theme.css`), is liye Network panel mein basename dekh kar galat-fehmi ka koi imkan nahi raha. Rename (`roles.css`/`semantic.css`) ki zaroorat bhi khatam — wo sirf is takraav ka hal thi. **Row 2026-09-02 ko band ki gayi, us waqt jab item 9 ne aakhri `<link>` bhi hata diya aur har page apni waahid entry file par aa gaya.** Us ki nasihat — *"quote full paths in every handover click-list"* — phir bhi achhi aadat hai aur `PROBES.md` mein zinda hai. ~~Original row:~~ **Two different files are both named `theme.css`, and the browser's Network panel shows only the basename — it cost a real false alarm on the first browser check of the epic.** `static/theme.css` (13787 bytes) is the OLD live stylesheet — `--fg`, `--brand`, `--app-bg`, `--sidebar-bg`, `--line`, `--panel` — still linked by the six unmigrated pages and deleted in UI-064. `static/css/01-settings/theme.css` (8682 bytes) is the NEW Tier 2 semantic-token file authored in UI-020 — `--color-canvas`, `--color-surface`, `--color-text` — and `main.css`:87 imports it into `layer(settings)`. On a migrated page the first is absent and the second **must** load; removing it would take the whole Modern palette with it. But Chrome/Edge list both as plain `theme.css` in the Name column, so `slo.html` reads at a glance as though the link this task removed is still there. Irfan hit exactly that at the UI-031 handover, checked it rather than assuming, and was right to: initiator `main.css:87` is genuinely a `theme.css` import — of the other file. **Telling them apart in the browser:** hover the row for the full path, or read the size — **8.7 kB is the new one, 13.8 kB the old** — those are content sizes; Chrome's Size column shows *transferred* on the top line with content beneath, so the panel may read ~9.0 kB and ~14.1 kB instead (Irfan saw 9.0). Either pair discriminates: 8.7-or-9.0 cannot be confused with 13.8-or-14.1. The two claims that look contradictory are both true at once: `static/theme.css`'s token names all resolve `<EMPTY>` on a migrated page, while `01-settings/theme.css`'s are populated. `01-settings/theme.css`'s own header already warns *"NOT the same file as static/theme.css"*, which was written for a session reading source — it does not help someone reading a Network panel | UI-031 2026-08-01, at Irfan's browser check | UI-031 owns one page's `<link>` block and the entry-file mechanism. The clean fix is renaming `01-settings/theme.css` (`roles.css` or `semantic.css` — it holds semantic roles, and the ITCSS layer already carries the word *settings*), which touches a reviewed Sprint-2 file plus `main.css`'s import and is a naming decision for Irfan, not a defect to patch inside a page migration (§12.2). Doing nothing is also defensible: the collision dies on its own in UI-064 when `static/theme.css` is deleted | **UI-064** resolves it by deletion with no work at all — but the ambiguity is live for the whole of UI-031's remaining four pages and all four of UI-032's, i.e. every browser check between here and Sprint 6. If Irfan wants it gone sooner, a rename is a small standalone task: one file, one `@import` line in `main.css`, no page touched, no ratchet movement. **Until then, quote full paths in every handover click-list** — `/static/css/01-settings/theme.css` vs `/static/theme.css`, never bare `theme.css`. That wording is what caused this |
| ~~D30~~ ✅ | **BAND 2026-09-02 — wo docs-only sweep ho gayi jo ye row maangti thi, aur chaaron jagah durust ki gayin** (row teen ginti thi; chautha `main.css`:117 ka *"still a plain `<link>` on 6 pages"* isi row ke apne 08-04 wale update mein darj tha): `pages/slo.css`, `pages/slo-health.css`, `main.css`:10-12 aur `main.css`:117. **Sweep ka jawab ab pehle se saaf tha, is liye ginti dobara likhne ki zaroorat hi nahi padi:** `/static/theme.css` UI-064 mein delete ho chuki hai aur item 9 (2026-09-02) ke baad **har page sirf apni entry file link karta hai** — yani "kitne pages ise link karte hain" ab hamesha **sifar** hai aur ye jumla dobara basi nahi ho sakta. Har jagah asal alfaz `~~strike~~` ke saath chhore gaye hain aur wajah likhi hai, taake koi ye na samjhe ke record chupke se badla gaya (§12 rule 11). Row ki apni hidayat — *"give the count **and** name the pages"* — is soorat mein la-yaani ho gayi, aur yehi is ka behtareen anjaam tha. ~~Original row:~~ **"The N unmigrated pages" is written in three places and is wrong in all three — the same conflation each time, and always in the sentence that hands `theme.css` over to the next session.** The number quoted is the count of pages that still **link** `/static/theme.css`; the noun attached to it says **unmigrated**. Those are different sets, because `landing.html` and `print.html` are unmigrated and never carried `theme.css` at all — which is **D9**, already on this board. Measured at each point in history rather than argued: at UI-031a `pages/slo.css`:38 says "six unmigrated pages" — 6 linked it, **8** were unmigrated; at UI-031b `pages/slo-health.css`:35 says "the five unmigrated pages" — 5 linked it, **7** were unmigrated; and **D29 itself**, two rows up, says "still linked by the six unmigrated pages" with the same split. The count is right every time and the noun is wrong every time, so nothing downstream has broken yet — but the sentence exists to tell a future session which pages still need migrating, and it gives the wrong list to anyone who trusts the words over the numbers. This is the **third** appearance of the epic's most-repeated failure shape (a number carried instead of counted): UI-020 was FAILED for "linked by 8 pages — it is 7", UI-030 for "the mockup's nav is 12 items — it is 11" | UI-031b 2026-08-01, by the review agent, while it was checking the *fourth* instance in `pages/library.css` — that one was fixed in-task because the task owned the file | The three stale instances sit in files owned by **completed, reviewed and committed tasks** (`pages/slo.css` UI-031a, `pages/slo-health.css` UI-031b, `DEFERRED.md` D29). §12.2: editing them from inside the library migration is a drive-by fix that makes this diff unreviewable, and rewriting a prior task's reviewed record is exactly what §12 exists to prevent. It is prose-only — no CSS, no metric, no page behaviour, so nothing is at risk while it waits | A single docs-only sweep, cheapest as its own small task in the UI-013a / UI-017a / UI-018a mould (they set the precedent that a self-contained correction gets its own reviewed commit). Or fold it into **UI-064**, which deletes `/static/theme.css` and makes all three sentences moot. Whoever takes it: give the count *and* name the pages, e.g. "still linked by index, taqseem, blueprint and bank — four of the six unmigrated pages", so the two sets can never be silently merged again. **UPDATE 2026-08-04 (UI-032, `bank`): both counts moved again and the stale sentences drifted further.** Measured, not argued: **3** pages still link `/static/theme.css` (`blueprint`, `index`, `taqseem`) and **5** are unmigrated (`blueprint`, `index`, `taqseem`, `landing`, `print`) — the gap is now two, not one, because `landing` and `print` never linked it (D9). So `DEFERRED.md`'s own D29 ("still linked by **the six unmigrated pages**"), `static/css/main.css`:10-12 ("the other eight pages still carry their own `app.css` + `theme.css`") and `main.css`:117 ("still a plain `<link>` on 6 pages") are each wrong in the count *as well as* the noun now. Still **not** fixed here (§12.2 — three files owned by committed tasks), but recorded rather than left to be discovered again. `pages/bank.css` was checked and does **not** reintroduce the phrasing |
| D31 | **`--color-text-muted` crosses WCAG AA on a TINTED surface: two `<label>`s in `.urdu-toggle-row` land at 4.44:1 — the first place the label token actually fails, not merely falls.** Same mechanism as D26, third page in a row: `03-elements/forms.css`:47's bare `label { font-size: var(--text-label); font-weight: var(--font-weight-strong); color: var(--color-text-muted) }` is an ELEMENT rule in `layer(elements)`, so it outranks `99-legacy/bank.css`:108's `.urdu-toggle-row label { margin: 0; font-size: 13px; font-weight: 600; color: var(--ink); cursor: pointer }` — a *class* rule in `layer(legacy)`, and more specific, which is exactly D21's point that layer order is decided before specificity. (The rule is quoted in full deliberately: its `font-weight: 600` is what makes the failing labels 12px/**600**, which is why the large-text exemption is discussed below at all. An abridged first draft of this row dropped it.) Measured on `bank.html` in headless Edge 151, both states, with the backdrop found by **walking the ancestor chain to the first non-transparent background rather than assumed** — D26's first draft failed for assuming one. **Method limit, stated so the next page does not inherit a wrong number:** the walk stops at the first background with alpha > 0 and does **not** composite translucent layers. Both backdrops here are fully opaque (`rgb(255,255,255)` and `rgb(245,247,251)`), so the figures are exact on this page — but pointed at a translucent surface, such as the `rgba(255,255,255,.07)` hover states `99-legacy/bank.css` paints in the sidebar, it would return the wrong colour. Composite before reusing the method, particularly on `index`. All 59 `<label>`s on this page take the token; **57 of them sit on `#ffffff` and land at 4.76:1, which PASSES** (before: 15.59:1 / 16.00:1). The two that fail sit on `.urdu-toggle-row`'s own `background: #F5F7FB` (`99-legacy/bank.css`:102): before `rgb(22,33,58)` on `rgb(245,247,251)` = **14.92:1**, after `rgb(100,116,139)` on the same = **4.44:1**, against AA's 4.5:1 for 12px/600 (the large-text exemption needs ≥18.66px bold or ≥24px, so it does not apply). The shortfall is 0.06 and it is still a fail. **The pattern this establishes is worth more than the two elements**: slate-500 was chosen against the white canvas and clears AA there with ~0.26 to spare, so *any* legacy surface tinted even slightly off-white takes the token below the line — `library`'s 44 labels cleared at 4.76:1 only because they are all on white. Every remaining page with a tinted panel will hit this | UI-032 `bank` 2026-08-04, in the browser | §12.2 — this task's scope is one page's `<link>` block plus the entry file. All three fixes belong to someone else: a darker muted role or a surface-aware token is `01-settings` (Sprint 2, closed); narrowing `forms.css`'s `label` so it sets size and weight but not colour edits a reviewed UI-021 file and changes all nine pages; and the right fix — a `.field` that owns its own label colour against the surface it sits on — is the field component. Patching `99-legacy/bank.css` is barred: that file is append-never | **PARTLY RESOLVED 2026-08-10 by UI-047e, and deliberately NOT closed.** Irfan took the one-line version this row offers: `--color-text-muted-strong` (`--slate-600`) in `01-settings/theme.css`, read by `03-elements/forms.css`s `label`. Measured after: the two `.urdu-toggle-row` labels go **4.44:1 -> 7.07:1**, and all 59 on `bank` plus 44 on `library` take it — `library` is LIVE and this changed it, 44 labels and nothing else, with `slo`/`slo-health`/`landing` at 0 deltas. **This row understated the problem and the correction matters more than the fix**: slate-500 fails AA at 12px/600 on THREE Tier 2 surfaces, not one — tinted 4.44, **`--color-canvas` 4.48** and `--color-surface-inset` 4.34 — against white 4.76 and sunken 4.60 which pass. Canvas is the page background, so this was never only two labels. slate-600 clears all five at 6.92-7.58. **STILL OPEN because two consumers keep the weak role**: `03-elements/typography.css`s `small` and `05-components/card.css`s `.pagehead p`, neither of which sets its own surface. Whoever takes UI-042 takes those. Separately and NOT fixed here: `bank`s migration moved one `<small>` from 5.41:1 to 4.44:1 — that is **D26**, it lands on every migrated page, and it is identical under both options | see also: **UI-042** (`field` component), with D26/D27 as the same family — a component in `layer(components)` outranks the element rule and can pick its colour per surface. If Irfan wants AA before Sprint 4 lands, the one-line version is a `--color-text-muted-strong` role used by `forms.css`'s `label`, which would lift all 59 at once. **Flagged at handover, not shipped silently** |
| ~~D32~~ ✅ | **RESOLVED 2026-08-08 — the check now reads markup; see the last column.** The finding, as recorded: **the orphan-TOKEN check reads stylesheets and never reads the markup, so a `var()` inside an inline `style=""` is invisible to it — and `blueprint` turns out to read NINE such tokens with no fallback.** `scripts/css_orphans.py` collects what `99-legacy/<page>.css` declares and reads; a token read from a `style="…var(--x)…"` attribute in the page's HTML is a read the script cannot see, so a page can measure 0 orphan tokens and still lose a value when `/static/theme.css` is unlinked. Found on `bank`, whose markup reads `--line` at `static/bank.html`:**99 and :594** *(this row said 98 and 593; the script's re-run located them and the citation was off by one)* — a name `99-legacy/bank.css` declares zero times — and measured live: 2 elements go `rgb(234,237,243)` → `rgb(226,232,240)`, i.e. down to the inline fallback. Harmless there **because** the fallback exists, which is the shape D9 records for `print`'s `--accent`; it is not harmless by luck of the stylesheet check. **The whole project was then swept rather than the one page argued about** — every page's inline `var()` reads, minus what its own legacy file declares, minus what the new tree declares: `slo`, `slo-health`, `library` **none — nothing already shipped is affected**; `bank` `--line` only, with a fallback; `index` **none**, so its 224 inline `style=""` attrs (the project's largest share) are a volume risk and not an exposure one — every name it reads inline is declared in `index.css`'s own `:root`; `taqseem`, `landing`, `print` none. **`blueprint` is the outlier and it is not close**: its markup reads `--accent`, `--accent-soft`, `--brand`, `--chip-bg`, `--fg`, `--green`, `--green-bg`, `--muted` and `--red` from inline styles, all `/static/theme.css`-only, and **not one of the nine carries a fallback**. So `blueprint`'s real exposure is 21 orphan tokens *plus* 20 orphan rules *plus* 9 unsupplied markup reads — the stylesheet-only figure understated it, and the page being HELD (2026-08-04) is the more obviously right call for it | UI-032 `bank` 2026-08-04, by the review agent, checking whether the entry file's "orphans no token" claim was true of the page or only of its stylesheet | §12.2 — this task owns one page's `<link>` block. Teaching `css_orphans.py` to parse markup is a change to a script two committed tasks depend on, and it would move published numbers, so it needs its own reviewed commit in the UI-017a / UI-018a mould. Nothing is at risk meanwhile: the sweep above is the answer for all nine pages as they stand today, and the only page it changes the picture for is already held | **DONE 2026-08-08, exactly as prescribed.** `css_orphans.py` gained `markup_read_names()` — the same `READ_RE` applied to inline `style=""` bodies, with HTML comments blanked first so a `style` inside `<!-- -->` is not counted as live. **Three new columns, rightmost, after every stylesheet figure**: `mkRead` (names read from markup), `mkOrph` (those the page's own legacy file and the new tree both fail to declare), `mkBare` (of those, no fallback). **Nothing was merged into the existing counts** — they are separate keys in `measure()` — and `--names` lists bare and has-fallback separately, which is the distinction that matters. **The re-run reproduced this row's recorded sweep cell for cell**, which is what makes the new code trustworthy rather than merely green: `slo` / `slo-health` / `library` / `taqseem` / `landing` / `print` **0**; `index` 6 markup reads and **0** exposure, all declared in its own `:root`; `bank` **1** — `--line`, with a fallback; `blueprint` **9**, all bare, and the nine names match one for one. **The script only reads markup — no `.html`, no legacy file and no CSS was touched** (`git status` showed five modified files: this script and four docs), and the ratchet is flat because no CSS changed |
| ~~D33~~ ✅ | **RESOLVED 2026-08-11 by UI-047f, and VERIFIED ON PAPER the same day.** `05-components/urdu.css` carries `.letterhead .school-ur` beside `.q-text .qt.rtl`, both reading `var(--leading-nastaliq)`. **Verified properly rather than assumed**: `school_name_ur` was temporarily filled with a real Nastaliq string, all three papers printed, and Irfan confirmed the letterhead sits on its own clean line with no overlap into the English name or the phone line. Measured alongside it in print media, webfont awaited: `line-height: normal`, box **47px**, ink extent **47px** — box and ink equal, which is what "no overlap" means numerically. The field was then restored to empty, because the Urdu string used was a plausible transliteration invented for the test and not AII's actual registered name; filling it for real is Irfan's to do. **TWO FINDINGS CAME OUT OF THAT TEST AND NEITHER WAS PREDICTED — see D38 and the note below.** **THE FRAMING IN THIS ROW WAS INCOMPLETE, and the correction matters more than the fix**: in PRINT media the letterhead already inherits `normal` from UI-044b's `@media print { body { line-height: normal } }`, so the `urdu.css` selector is **redundant in print** — measured both ways, identical at 47px with and without it. It earns its place on SCREEN, where `@media print` does not apply and the letterhead would otherwise take `--leading-body`. That last half is a reading of the cascade and was **not** measured. **ORIGINAL FINDING:** `print.html`'s Urdu letterhead line will overlap the moment the page is migrated AND a school fills in its Urdu name — the same mechanism `bank` is HELD for, on the Ctrl+P page.** `99-legacy/print.css`:126 `.letterhead .school-ur` sets `"Noto Nastaliq Urdu"` at 19px and **no `line-height`**, and no ancestor supplies one either — `body`:24, `.sheet`:112 and `.letterhead`:118 were each read and none declares it. So it inherits, and `03-elements/typography.css`:41's `body { line-height: var(--leading-body) }` sits in `layer(elements)` and beats `layer(legacy)`. Measured in **print media with the webfont loaded first**: `normal` → **27.55px while the glyph line rect stays 47px**, i.e. a 47px ink extent inside a 28px box. **Nothing is broken today on two counts**, and both must hold for that to stay true: `print.html` is at HEAD and unmigrated, **and** `school_name_ur` is empty in this database. **Either one changing exposes it** — and the migration is already measured, approved on sight and parked (`docs/ui/parked-print.css`), so the likelier trigger is shipping that. Note what this is NOT: the Urdu verified in Irfan's print preview (`Name / نام:`, `تمام سوالات کے جواب دیں۔`) is a **system naskh fallback inside `.label`/`.instructions`**, both computing to `"IBM Plex Sans", system-ui, sans-serif` — 16px line box against 30px for the same string forced into Nastaliq — so it fits a 1.45 leading and is unaffected. `.school-ur` is the page's only genuinely-Nastaliq element, and `print.css`'s five `!important` declarations are all `display: none` and protect nothing here | UI-032 `print` 2026-08-05, measured while checking whether `bank`'s hold applied to this page | §12.2 — the task owned one page's `<link>` block, and this needs a **decision about Nastaliq leading**, not a one-line patch. It is the same decision `bank` is held for, so patching it here in isolation would pre-empt that and probably diverge from it. `99-legacy/print.css` is also append-never | **Whichever task takes `bank`'s Nastaliq decision must take this with it — they are one problem in two places.** That task does not exist yet; see **D34**. **Verify by filling `school_name_ur` and printing** — no automated check in this epic reports it. **UPDATE 2026-08-05 (UI-044 part 1): the mechanism now has a home and this row is a one-line job whenever `print` migrates.** `05-components/urdu.css` landed in `layer(components)` and fixed the identical defect on `bank.css`:229 with `line-height: var(--leading-nastaliq)` (`normal`, i.e. the font's own 2.53x metrics). It was deliberately NOT extended to `.letterhead .school-ur` here, because `print.html` is HELD and unmigrated — it does not link `main.css`, so the rule could not reach it and would have been dead CSS. **Add that selector to `05-components/urdu.css` in the same commit that migrates `print`**; the file's header records this. Note the value question is already settled for Urdu generally, so this needs no new decision |
| ~~D36~~ ✅ | **RESOLVED 2026-08-11 by UI-047f**, in the commit this row required. The fix went live with the migration and was re-measured on the way in: all three papers at **2 / 6 / 7**, drift 0, an A4 MediaBox on every PDF — measured once before the D33 selector was added and again after, identical both times. **Irfan then printed all three on real paper and confirmed it**, which is the check this row said `Page.printToPDF` could not substitute for. **ORIGINAL FINDING:** migrating `print` makes one of three real papers gain a printed page — `0d04c750` goes 2 pages to 3. Measured 2026-08-05 by performing the swap, measuring in print media and reverting; PDFs produced with `Page.printToPDF`, pages counted from the PDF rather than by dividing a height. All three papers, drift 0 both sides: `0d04c750` (20Q, 0 images) **2 -> 3 pages**, sheet 2170.8 -> 2300.7px (**+129.9px, +6.0%**); `a5015cda` (20Q, 19 images) 6 -> 6, +129.9px (+2.4%); `9ade2655` (25Q, 25 images) 7 -> 7, +101.5px (+1.6%). **The cause is not the margin** — `.sheet`'s padding is 52.9134px (14mm) on all four sides before *and* after, and `html`/`body`/`.print-main` are 0 on every side both ways (D35). It is **D21/F3's line-height growth**: `03-elements/typography.css`:41's `body { line-height: var(--leading-body) }` sits in `layer(elements)` and beats `layer(legacy)`, every line box opens up, the sheet grows a few percent, and a paper already sitting near an A4 boundary crosses it. **F3 predicted exactly this and could not measure it** — it read "pagination held: 7 pages before, 7 after ... that is one paper's margin, not a guarantee. Nothing measured how near any other paper sits to a page boundary, and a paper closer to one could cross it." Three papers were enough to find one. **The consequence is physical**: one more sheet of paper per exam, per student, on any paper near a boundary — on the Ctrl+P page, which is the highest-consequence one in the epic. **This also explains a number the board could not**: `MEASURED.md`'s test-data table gave `0d04c750` as 3 pages while HEAD measures 2. That figure was most likely recorded from the *migrated* state in the first `print` session, so the table's disagreement with HEAD was itself this regression, unrecognised | UI-032 `print` 2026-08-05, settling D35 | It is **the leading decision**, not a `print` fix — the same decision `bank` is HELD for and `print`'s D33 needs. Patching it on this page alone would pre-empt that and probably diverge from it, and `99-legacy/print.css` is append-never. `print` is HELD, so nothing is shipping meanwhile | **UI-044** (type + leading), which owns the leading decision for `bank`, D33 and this together. **Verify by re-running the margin harness after UI-044** and confirming all three papers return to HEAD's page counts (2 / 6 / 7) — a leading fix that leaves `0d04c750` at 3 has not settled this. Note the harness measures `Page.printToPDF`, which is the right tool for CSS and is **not** proof of a physical print; check a real print preview before `print` is unheld. **UPDATE 2026-08-05 (UI-044b): THE FIX IS WRITTEN AND PROOF-TESTED, AND IT IS NOT LIVE.** `docs/ui/parked-print.css` now carries `@layer components { @media print { body { line-height: normal } } }`. Proven on a temporary swap: all three papers back to HEAD's counts, **2 / 6 / 7**, drift 0. `font-size` was measured alongside it and changes nothing, so it is not in the rule. **This row stays OPEN deliberately** — the file sits in `docs/`, not `static/`, so the ratchet does not see it (`shared_css_lines` 1685 unchanged) and no page can load it. It activates in the same commit that migrates `print`; **move this row to Resolved then, not now.** Note also what it does not restore: HEAD's *page count*, not HEAD's height — 2170.81px HEAD, 2168.61px fixed, 2.2px shorter, because the base font-size stays at the new tree's 15px. **The rule was measured in `03-elements/typography.css` first and moved out on the numbers**: there it took `slo`'s printed output from 22 pages to 20 and changed all three live pages' print-media leading. In the entry file it reaches `print.html` alone — 0 deltas on all three, in print media |
| D38 | **`print.html`'s page counts are DATA-DEPENDENT, and the board's 2 / 6 / 7 silently assumes an empty Urdu school name.** Measured 2026-08-11 while verifying D33: with `school_name_ur` empty the three test papers are **2 / 6 / 7**; with a real Nastaliq name filled in they are **3 / 6 / 7**. `0d04c750` gains a page. **The cause is the data, not any CSS fix** — proven by removing `05-components/urdu.css`'s `.letterhead .school-ur` selector and re-measuring: **3 pages either way**, so it is not D33's fix and not D36's. Filling the field simply adds a 47px Nastaliq line to the letterhead on a paper that D36 already recorded as sitting nearest an A4 boundary (+6.0%). **This is NOT a regression and nothing is broken** — it is the same physical consequence D36 describes (one more sheet per exam per student) arriving through content instead of through leading, and it is the correct rendering of more content. | UI-047f 2026-08-11, measured in print media with `Page.printToPDF`, both with and without the D33 selector | Nothing to fix, so nothing was deferred — this row exists because **the figure 2 / 6 / 7 is quoted in six places on this board without its precondition**, and the next session to re-derive it with a school name filled in would read a mismatch as a regression. | **No task. It is a caveat on a number, not a defect.** Whoever quotes 2 / 6 / 7 should quote the precondition with it. If a school ever fills its Urdu name for real, re-derive the counts before treating any of them as a baseline |
| D37 | **A literal fallback on every component token (`var(--color-action, <hex>)`) was proposed, and measurement declined it.** The premise was that `layer(settings)` could lose to legacy, leaving a component unpainted. **That direction is not possible on this branch:** `main.css`:42 declares `@layer legacy, settings, generic, elements, objects, components, utilities` — settings comes *after* legacy and later layers win, which is how the new palette takes over from the six pages that shadow `theme.css`'s token names in the first place. **State that rule precisely, because the loose version is not true:** later-layer-wins holds for *normal* declarations only. An `!important` declaration reverses layer order entirely, so an `!important` custom property in `layer(legacy)` WOULD beat `layer(settings)`. Checked at review — **no legacy file and no `theme.css` declares an `!important` custom property**, so the conclusion holds; it holds by measurement, not by the rule alone. A future legacy edit that adds one would break it silently. Measured 2026-08-06: of the **12 tokens `05-components/btn.css` reads, not one** is defined in any of the nine legacy files or in `static/theme.css` — the legacy names are `--primary`/`--ink`/`--fg`/`--line`/`--tint`, the new tier is `--color-*`, and they do not overlap. **Two** of the twelve are demonstrably resolving on all three live pages today — body computes 15px (`--text-body`) and `slo`'s `a.btn-ghost` computes `rgb(15, 23, 42)` (`--color-text`) — with body's 21.75px line box showing `--leading-body` resolving alongside them, which is a settings-layer token but **not** one of `btn.css`'s twelve (this file declares no `line-height`). Review corrected an earlier "three of the twelve" that folded it in. **The two real bleed directions are already recorded and neither is fixable by a fallback:** unlayered `static/theme.css` beats every layer while a page still links it (D19/D20, four held pages), and an inline `style` on `documentElement` beats everything including layers (UI-018, `print`'s three knobs) — both touch different names. And if `@layer` ever hard-fails, the component's own rules are discarded with it, so a fallback inside them cannot run. | UI-041 2026-08-06 | **Do not call this "the D35 pattern" — D35 is the investigation that retired this exact mechanism.** D35 reported a `--space-*` regression on `print` "attributed to `layer(settings)` losing to a legacy unlayered `:root`"; the swap was performed, the AFTER half measured, and the printed margin did not move at all, `.count-grid` exists nowhere in the repo, and no legacy file reads `--space-*`. D31 is not a precedent either — it is a WCAG contrast failure caused by an element selector outranking a legacy class rule (D21), not by a token failing to resolve. | **No home — deliberately.** Tier 1 `01-settings/tokens.css` already IS the single literal fallback point; every Tier 2 role resolves to a Tier 1 primitive. A second literal in Tier 3 creates two sources of truth, which is what this epic exists to remove. If defence-in-depth is ever wanted anyway, its home is `01-settings/`, never `05-components/`. **Reopen only with a measured case of a component token failing to resolve on a real page** |
| D41 | **`.pagehead p` fails WCAG AA on the page background — by 0.02 — and it has been live on `blueprint` since 2026-08-15. It is not a defect UI-060 introduced; S1 is simply the second page to reach it.** Measured 2026-08-22 in headless Edge 151, 1280x900, drift 0: `05-components/card.css`:104 gives `.pagehead p` `var(--color-text-muted)`, which computes to **`rgb(100, 116, 139)`** at 13.5px, against a measured body background of **`rgb(247, 248, 251)`** — contrast **4.48:1** where AA for normal text is **4.5:1**. The legacy rule it replaces, `99-legacy/slo.css`'s `.page-head p { color: var(--muted) }` at `rgb(91, 102, 120)`, measured **5.47:1** and passed. So migrating this one element **lowered** contrast, and both the background and both foregrounds were read out of the probe snapshot rather than out of a token file — `--color-canvas` is not `rgb(255,255,255)` here and computing against white gives 4.76:1, which passes and is the wrong answer. **`blueprint` already renders the identical value**: verified in the same run, on the BEFORE snapshot, `blueprint`'s `.pagehead p` is `rgb(100, 116, 139)` at 13.5px — so the failure predates this task and the count of affected pages went 1 -> 2, it did not go 0 -> 1. **This is D31's exact shape at a different selector** (`bank`'s two `.urdu-toggle-row` labels, 4.44:1 against 4.5:1), which is the reason to record it as its own row rather than fold it in: two independent Tier 2 muted-text failures within 0.06 of the threshold is a pattern in the token, not two coincidences in two components. | UI-060 S1, 2026-08-22, measured while gating the `.page-head` -> `.pagehead` migration | **The fix is a Tier 2 token change and it reaches every page.** `--color-text-muted` is read well beyond `.pagehead`, so darkening it is a palette decision with a nine-page blast radius, not a slice of a one-page drain. Taking it inside S1 would have made a 7-delta migration into an unmeasured global repaint — the exact shape this epic has been bitten by. Note the honest limit: **nobody has looked at either page in a browser for this**; 4.48 vs 4.5 is a computed ratio, not a legibility complaint. | **No task yet — and it should be one task with D31, not two.** Both are `--color-text-muted`-family failures a hair under AA; whoever takes the palette decision should re-measure every muted-text surface at once and set the token from the worst case, rather than nudging it twice. **Verify by re-running the contrast computation against the MEASURED background of each page** — not against white, and not against the token's nominal value. Until then `blueprint` and `slo` both ship at 4.48:1, which is the status quo `blueprint` has had for a week and is recorded here rather than silently inherited |

---

## Resolved

| ID | What | Resolved by |
|---|---|---|
| ~~D14~~ ✅ | **RESOLVED 2026-08-29 by UI-072 — AND THE ROW WAS WRONG ABOUT THE ONE THING THAT MATTERED.** It said `.pg-btn`'s `color: var(--text)` was "already inert". **It was not inert; it was load-bearing.** An unresolvable `var()` makes its declaration invalid at computed-value time, which on an INHERITED property such as `color` resolves to `inherit` — and on a `<button>` that overrides the UA's `color: buttontext`. Deleting it was measured at **25 deltas, rgb(15,23,42) → rgb(0,0,0)**. It is now an explicit `color: inherit`: same pixels, no unresolvable reference. The repo now has **zero** unresolved `var()` on any page. **The general lesson: "resolves to nothing" and "does nothing" are different claims** | **UI-072, 2026-08-29** |
| ~~D53~~ ✅ | **RESOLVED 2026-08-29 by UI-072**, with the small fix this row itself proposed rather than the probe change it first asked for. `css_type_diff` now prints `… and N more — re-run with --all to see them` after the cap, and `--all` lifts it. The row's own history is the argument: a reading taken from the truncated list was written into the board as "0 deltas" when the count said 30 | **UI-072, 2026-08-29** |
| ~~D56~~ ✅ | **RESOLVED 2026-08-29 by UI-072.** `plan` is now in the page list of both `css_type_probe.mjs` and `css_state_probe.mjs`, with a comment saying why it mattered most: it is the one page whose ONLY stylesheet is the new tree, so every component change had been reaching it unmeasured | **UI-072, 2026-08-29** |
| ~~D58~~ ✅ | **RESOLVED 2026-08-29 by UI-072.** `pages/index.css`'s comment claimed 99-legacy's `.brand .tag` "already wins font-size and color at 0,2,0". Layer order is decided before specificity and that rule is in `layer(components)` while legacy is the weakest layer, so it never won — measured: index's `.brand .tag` computes 11px and `--color-action`, i.e. the components rule, and only `letter-spacing` ever survived from legacy. Comment corrected and it now names `.topbar .tag` as untested for the same reason | **UI-072, 2026-08-29** |
| ~~D49~~ ✅ | **RESOLVED 2026-08-28 — and the answer was the SECOND of the two candidates: the probe is fine, the CSS is dead.** The row said `css_state_probe` might be blind to `cursor` under a `disabled` attribute, because `slo.css`'s `.btn:disabled { opacity:.5; cursor:not-allowed }` produced an `opacity` delta and no `cursor` delta from the same rule on the same element. A four-button control page — all disabled, differing ONLY in which layer carries the `:disabled` rule — reads `pointer` when that rule is in `layer(legacy)` and `not-allowed` in `layer(elements)`, `layer(components)` and unlayered, with `opacity: 0.5` in all four. CDP reads the property correctly; what differs is the competition. `03-elements/forms.css`:155 declares a bare `button { cursor: pointer }` in `layer(elements)`, which beats `layer(legacy)` whatever the specificity, while nothing at all contests `opacity`. **So all six `cursor: not-allowed` declarations in `99-legacy/` are inert** — blueprint:48, index:96, library:185, print:74, print:83, slo:31, every one of them on a `<button>` element that `forms.css` matches. UI-063's cursor decision has never been true on any page. Note written into `PROBES.md` as rule 9 (the row asked for "beside rules 10 and 11"; there are eight). **What is NOT resolved is the consequence** — making `not-allowed` actually apply means lifting the declaration above `layer(legacy)`, which is item 7's `btn` decision and is scoped there | **UI-069a, 2026-08-28.** Method: `PROGRESS.md` 2026-08-28; rule: `PROBES.md` 9 |
| D39 | The frozen inventory matched `id`, `onclick`, `name` and `data-*` only, leaving `onchange` and `oninput` outside the guard `CLAUDE.md` §12.7 exists to provide | **Done 2026-08-23.** `FROZEN_ATTR_RE` now builds from a named `FROZEN_HANDLERS` tuple; inventory **699 → 780 (+81)** with **zero old entries lost** (diffed per page before writing, so the widening is provably additive). **The row's own numbers were stale** — it said 123/59/17 = 76; re-counted on the day it was fixed: **`onclick` 125, `onchange` 61, `oninput` 20 = 81 unguarded**, 39% of all handlers. Same disease as the P0 rows in `ROADMAP.md`; the count in a parking-lot row is a timestamp, not a fact. **All 81 were inspected before `--write`, and they split two ways:** 57 in plain markup, and **24 inside `<script>` `innerHTML` templates** (`onchange="onTypeChange(${i}, this)"`). The template ones are literal text in the source, so freezing them is stable and still catches a rename of the handler. One is ugly and worth knowing about — `blueprint.html`:410 is a JS concatenation (`setPinSection(\'' + escAttr(q.id) + '\', this)`) that the regex swallows whole; it guards the name correctly but reads badly in `BASELINE.json`. **Guard proven, not assumed:** `oninput="setPrintRange()"` — the exact handler that went in unguarded on 2026-08-20 — was renamed in a temp write, `--check` exited 1 with `MISSING ... renaming this breaks a handler silently`, and the file was restored (`git status` clean). `onsubmit`/`onkey*`/`onblur`/`onfocus` are listed at zero occurrences so the next page to grow one is guarded from its first commit. Ratchet green, ruff clean, 1055 pass |
| D34 | Three of the six HELD pages needed work that no Sprint 4 task ID owned — `blueprint` (nav/shell), `bank` (the Nastaliq leading decision) and `landing` (a display/hero type step). Following `PLAN.md`'s Sprint 4 as written would have ended the sprint with them still held | **Resolved 2026-08-05, docs only.** `PLAN.md`'s Sprint 4 re-scoped from 4 tasks to **7** and re-ordered from "duplication count" to **"held pages released"**. Three new IDs: **UI-044** type + leading (releases `bank` and `print`, settles D33 and D36), **UI-045** display/hero type step (releases `landing`), **UI-046** nav + shell (releases `blueprint`, completes `index`). New order: UI-044, UI-045, UI-041, UI-046, UI-040, then UI-042 and UI-043, neither of which any held page waits on. UI-044+045 go first because together they release **three of the six** and each completes its pages outright, where UI-041 is needed by two pages and completes neither alone. The per-page blocker map is in `PLAN.md`'s Sprint 4 section. **Note what this resolution does NOT contain:** an earlier plan treated a `--space-*` layer fix as the foundation that would open several pages at once — **D35 measured that mechanism and it does not exist on this branch**, so no task was scheduled for it |
| D35 | A space-token regression was reported on `print` — a right margin moving 48px to 24px and a `.count-grid` collapsing on Q10, attributed to `--space-*` in `layer(settings)` losing to a legacy unlayered `:root` | **Retired 2026-08-05: the mechanism does not exist on this branch, measured rather than argued.** The swap was performed, the AFTER half measured in print media on three real papers, and the tree reverted; `git status` clean, ratchet flat. **The margin does not move**: `.sheet` padding is 52.9134px (= exactly 14mm) on all four sides before *and* after on all three papers, `html`/`body`/`.print-main` are 0 on every side both ways, **zero box deltas land on the margin chain**, and the three knobs resolve identically. There is no 48px or 24px anywhere on that chain. `.count-grid` exists nowhere in the repo, no legacy file reads `--space-*` (zero matches across all nine), and `main.css`:42 puts `legacy` first and weakest with `settings` above it. **The measurement earned two things beyond the retirement.** (1) **The 48 padding deltas are now named** — the gap `MEASURED.md` called the deciding one. They are 12 elements x 4 sides, every one a form control taking `03-elements/forms.css`:68's `padding: 9px 11px`: `textarea#ef_question_en`, `textarea#ef_question_ur`, `input#ef_correct_en`, `input#ef_correct_ur`, `input#ef_marks`, `select#ef_answer_lines`, `select#ef_image_size`, `input#efStripSearch`, `select#efStripQType`, `input#libPickerSearch`, `select#libPickerQType`, `select#libPickerSize`. All twelve sit inside the edit modal, topic strip and library picker — `.no-print` chrome — so **their effect on the printed artefact is zero**. (2) **`@page` survives the layer import**, verified two independent ways: an import-aware CSSOM walk finds `@page { size: a4; margin: 0px }` at `layer=legacy`, and all six PDFs carry an identical A4 MediaBox (594.96 x 841.92) — had `@page` stopped applying, `preferCSSPageSize` would have fallen back to Letter. **A measurement defect was caught before it became a finding:** the first probe reported `@page` as absent after the swap because its walker recursed into `rule.cssRules` and a `CSSImportRule` exposes its contents as `.styleSheet` — an imported file was never entered. Corrected, then re-verified. **What the run DID surface is real and is now D36**: `0d04c750` gains a printed page, 2 to 3 |
| ~~D1~~ ✅ | `@layer` adoption pending a browser check | **Adopted 2026-07-28.** Floor is Edge/Chrome 99 (Mar 2022) and Edge auto-updates, so the deployment clears it by years. Reversible in one line if anything ever renders unstyled. |
| ~~D6~~ ✅ | `config/brand.json` `primary`/`navy` don't match the Modern palette | **Done in UI-003** (`2eba2f8`). Deleted rather than reconciled — nothing consumed them, re-verified at implementation: `brand.js` reads only `full_name`/`tagline`, no Python indexes the keys, no test touches the endpoint. The `--navy` declarations in the pages are local CSS vars, not reads of `brand.json`. |
